# M1 gap audit — 2026-07-02

**What this is.** A full audit of the tracer app (`apps/desktop-python/`, at
commit `a02ba09`) against every read-only M1 requirement in
`docs/product-spec.md` (§9 M1 definition, §5 v1 IN list, §2 non-negotiables,
§7 budgets, §4 compat contract, §3 data model). Produced by a 17-agent
audit workflow — 16 area auditors with file:line evidence plus a
completeness critic — and used to file the M1 bead tree on 2026-07-02.

**Verdict in one line:** 34 requirements done, 35 partial,
86 missing (sizes: S=hours, M=~day, L=multi-day, XL=needs design first).

**Bead map** (epics filed from this audit):

| Epic | Covers |
|---|---|
| `fauxcasa-cam` | M1 ingest completion (faces/people, contacts.xml, .pal, db3 rescue, EXIF/XMP, adopt-mode backfill, import report) |
| `fauxcasa-q6l` | M1 browse UI (multi-select, tray, slideshow, 1:1 zoom, peek, search upgrades, keymap, sort modes) |
| `fauxcasa-v46` | M1 formats (RAW, video, stills matrix) |
| `fauxcasa-i92` | M1 decode isolation per `docs/decode-threat-model.md` |
| `fauxcasa-ed5` | M1 gates (survey cross-check, corpus enrichment, §7 CI gates, multi-root design) |
| standalone | spec-gap beads `fauxcasa-07w/-1jb/-o66/-79b/-64z/-mqi` |

Statuses below reflect the code at `a02ba09`; they age as beads close.

---

## .picasa.ini ingest completeness — spec §4 "Read: everything" (ini), M1 read-only display slice

### ✅ done (S) Legacy ini file names: read .picasa.ini first, fall back to Picasa.ini / picasa.ini (format doc 'Naming history', §4 'and legacy Picasa.ini')

- **Evidence:** apps/desktop-python/catalog.py:50 (INI_NAMES tuple with all three casings), catalog.py:177-185 (_read_folder_ini tries each in order)
- **Notes:** First-found-wins with no merge when both names coexist in one folder — matches the format doc's own guidance (docs/research/picasa-ini-format.md:23 'Read .picasa.ini first, fall back to Picasa.ini'). No test in test_tracer.py exercises the legacy names (all fixtures write .picasa.ini); an hour of test coverage would close that. Windows hidden+system attributes on the file are handled implicitly (Path.is_file/read_bytes ignore attributes).

### ✅ done (S) Robust ini parsing: duplicate sections AND keys preserved, [(null)]/garbage tolerated, dynamic key names, fail-soft per-line never per-file, BOM, arbitrary section order (format doc 'Robustness requirements'; §4 'fail soft per-line, never per-file')

- **Evidence:** scripts/picasa_db.py:628-678 (custom parser, not configparser; anomalies by line number; no comment markers), catalog.py:188-203 (_section_map merges duplicate sections first-key-wins), test_tracer.py:318-326 (dup section + odd-case key test)
- **Notes:** Byte-reversed garbage lines and [(null)] sections degrade to anomalies/unknown sections without crashing. Unknown sections/keys are preserved in the parse (PicasaIni.sections) — irrelevant to byte-faithful write-back until M2, since the app never writes inis.

### 🟡 partial (S) Encoding: [encoding] utf8=1 marker; Picasa-2-era files in locale codepage; mixed encodings inside one file decoded byte-preservingly (format doc lines 31, 117-119)

- **Evidence:** scripts/picasa_db.py:651-653 (BOM strip + UTF-8/surrogateescape decode — bytes always survive); no codepage fallback anywhere; [encoding] section is never consulted
- **Gap:** Legacy locale-codepage (cp1252 etc.) captions/keywords/album names decode to surrogates and will render as mojibake in the M1 UI — byte preservation satisfies round-trip but not display. Add a per-file (or per-line) fallback: strict UTF-8 first, then locale codepage when [encoding] is absent or the bytes fail UTF-8.
- **Notes:** Matters for exactly the target user: a multi-decade archive whose oldest folders predate Picasa 3.0 and carry non-ASCII captions.

### ✅ done (S) star=yes read (presence-only, never star=no) and displayed — grid badge, Starred collection, status bar (M1 'stars displayed')

- **Evidence:** catalog.py:213-214+303 (_flag, trimmed case-insensitive 'yes'), grid.py:589-593 (star badge painted per tile), main.py:798-801 (Starred sidebar node with count), main.py:857-860 (starred view filter), viewer.py:250, test_tracer.py:192, 1536-1577 (hidden-starred reveal interplay)
- **Notes:** Reveal-mode interaction with hidden starred photos is tested.

### 🟡 partial (S) Legacy star=yes imports as 1 star into the 0–5 stackable-star model (§3 'Star authority', §5 Organize)

- **Evidence:** catalog.py:79 (Photo.star is a bool, not a count); no 0–5 model exists anywhere in the app
- **Gap:** The 0–5 count model and the star=yes→1 mapping don't exist; the tracer displays binary starred/unstarred. Adequate for M1 read-only display (a 1-star and a starred badge are visually equivalent claims), but the catalog schema will need a count field (+ CATALOG_VERSION bump) before M2's star writes land.
- **Notes:** Deliberately deferring the count to M2 is defensible; flagging so the schema change is planned, not discovered.

### ✅ done (S) caption= read with §4 tier-1 precedence (ini caption only for formats with no XMP/IPTC home; in-file wins for JPEG) and surfaced in search/status/viewer

- **Evidence:** catalog.py:304 (ini read), catalog.py:1-23 module docstring + inmeta.py:1-32 (precedence rationale), test_tracer.py:728-744 (in-file overrides ini), 747-758 (ini kept when no in-file), 1406-1424 (empty in-file caption doesn't clobber ini); surfaced main.py:880-887 (search), main.py:909-912 (status bar), viewer.py:252-253
- **Notes:** Known documented caveat: adopt mode (--thumbs) skips the indexer so its catalog stays ini-only for JPEG captions (catalog.py:20-23, README:152-157) — acceptable for the benchmark path, but a real library adopted this way silently shows the wrong (ini) caption tier; worth a UI notice eventually (N7 spirit).

### ✅ done (S) keywords= read (CSV, non-JPEG home; JPEG keywords from IPTC/XMP win) and surfaced in search + status bar

- **Evidence:** catalog.py:305-309 (CSV split, trimmed, empties dropped), main.py:884-886 (search over keywords), main.py:911-912 (status bar #tags), inmeta.py precedence tests as above

### ✅ done (S) rotate=rotate(N) quarter-turns applied to every display path, composed on top of EXIF orientation (format doc: 'EXIF orientation handling is the consumer's job')

- **Evidence:** catalog.py:310-315 (parse_rotate %4, fail-soft), grid.py:197-199 + 378-381 (thumb rotate at decode), viewer.py:88-171 (preview + original compose rotate on EXIF-upright image), test_tracer.py:1267-1285, README.md:94-113 (policy)
- **Notes:** The doc's warning that rotate= does NOT transform faces/crop coords is currently moot (neither is ingested) but must be honored when faces/crop land — coordinates are relative to stored pixels.

### ✅ done (S) hidden=yes per-photo hide, cosmetic and inspectable (one filter surface, N7/footgun 10)

- **Evidence:** catalog.py:316+325-327 (hidden → visible=False), main.py:483-487 ('Show hidden' toggle), main.py:700-733 (reveal preserves active view), grid.py:81+585-588 (veiled rendering), catalog.py:206-210 (stash folders .picasaoriginals/Originals treated likewise), tests test_tracer.py:1451-1620

### 🟡 partial (S) Folder-level Hidden Folders collection: [Picasa] P2category=Hidden Folders hides the whole folder (oracle fixture 032); legacy category= sibling key per format doc line 30

- **Evidence:** catalog.py:217-239 (_is_folder_hidden, oracle-verified per comment), catalog.py:296-299 (wired at scan), catalog.py:384-388+426-427 (persisted, CATALOG_VERSION 3), test_tracer.py:1758-1761; gap: catalog.py:237 checks ONLY 'P2category'
- **Gap:** The older `category=` key (format doc: '`category=` / `P2category=` (newer)') is not checked — a pre-3.9-era ini carrying category=Hidden Folders would show its photos. One-line fix + test; ideally confirm against an oracle/legacy sample which key older Picasa wrote.
- **Notes:** README.md:145-146 is STALE — it still says the folder-level category 'is still ignored'; the code honors it. Fix the README line. Other category values (Downloaded Albums~<uid>, Exported Pictures, Projects) are correctly treated as not-hidden.

### 🟡 partial (S) albums= membership tokens + [.album:<32hex>] definitions (name, date ISO-8601, description, location, token, <account>_lh); orphaned tokens skipped per documented ini/DB drift; albums as pure references in sidebar (M1 'albums displayed')

- **Evidence:** catalog.py:317-321 (tokens lowercased), catalog.py:329-357 (album sections collected, orphan tokens silently skipped, members resolved), main.py:826-834 (Albums sidebar), main.py:861-864 (album view + counts), test_tracer.py:176-212
- **Gap:** Read-but-unsurfaced: Album.date and Album.description are ingested and persisted but appear nowhere in the UI (sidebar shows name+count only; §5 names year grouping for albums). Unread: location=, token= (redundant with section id), <account>_lh (web-album relic — fine to skip). Duplicate diverging definitions of one uid resolve first-wins-in-walk-order, self-documented as arbitrary (catalog.py:333-334) — needs an oracle fixture to pin Picasa's rule. Album member ORDER is inherently absent from ini (spec §3: order lives in db3/tier-2) — that's the db3-rescue area, not an ini gap; members currently appear in catalog walk order.
- **Notes:** Nameless album sections fall back to uid[:8] (catalog.py:344) — reasonable.

### 🟡 partial (S) [Picasa] folder-level keys: name= (display name), description=, date= (OLE double), location=, link=, <account>_lh= — 'folder name/description' claimed surfaced

- **Evidence:** name= deliberately unused with rationale (catalog.py:288-292: on-disk name is truth per N1, ini name= goes stale on renames — a defensible product call); description= ingested catalog.py:296-299, persisted catalog.py:420-422, tested test_tracer.py:207+383; but NOT rendered anywhere — grid header shows title+count only (grid.py:621), sidebar shows title+count (main.py:822)
- **Gap:** Folder description is read-but-unsurfaced: add it to the group header, a tooltip, or the status bar (README's 'folder names/descriptions' wired claim is half-true). date=/location=/link=/<account>_lh= unread — date= (folder date) is the only one with a plausible v1 display/sort use (§5 per-folder sort modes); the rest are web-era relics safe to leave as preserved-by-not-writing.

### ❌ missing (L) faces= rects + contact ids ingested and displayed — M1 explicitly lists 'faces displayed'; §4 precedence: ini faces merge with XMP/contacts.xml/db3

- **Evidence:** Validated parser exists (scripts/picasa_db.py:719-739 parse_faces, zero-pad + UNKNOWN_CONTACT semantics; rect64 decode picasa_db.py:686-712 with leading-zero-strip handling) but nothing in the app calls it: no 'faces' reference in catalog.py/main.py/viewer.py/grid.py (grep confirms only docstring mentions); Photo dataclass (catalog.py:74-92) has no faces field
- **Gap:** The single largest ini-ingest gap for the M1 definition. Needs: Photo.faces ingest (rects + padded contact ids, UNKNOWN_CONTACT handling), catalog persistence (+version bump), name resolution via [Contacts2] (separate finding) merged with contacts.xml/XMP per §4, and a display surface (viewer face-region overlay and/or people list; coordinate math must account for EXIF orientation + rotate= NOT transforming stored-pixel coords). Ingest alone is M; ingest+names+display is the L.

### ❌ missing (M) [Contacts2] (and legacy [Contacts]) sections: contact id → display name, ids inherited downward from ancestor folders' inis; feeds face names and §5 search-over-people

- **Evidence:** No Contacts handling anywhere in apps/desktop-python (grep for 'Contacts' hits only README/docstrings); picasa_db.py parses the sections generically but nothing consumes them; search (main.py:880-887) covers filename/caption/keywords only — no people
- **Gap:** Parse [Contacts2] ('<16-hex>=<Name>;;' split on ';' field 0) per folder, propagate ids downward per the inheritance rule, keep [Contacts] legacy ids for the contacts.xml join (contacts.xml itself is a sibling ingest area), zero-pad face ids before joining, and index names into search. Blocked-with faces display; independent value for people search.

### ❌ missing (S) geotag=lat,lon read + displayed — §3: 'geotag (v1: read, preserve, display — badge + coordinates readout)'; §5: thumbnail corner badges include geotag; M1 = full ingest

- **Evidence:** No geotag reference in the app (grep hits only catalog.py:27 docstring admitting the gap and README:149); survey counts it (picasa_db.py:1329-1330) so the M1 zero-ingest-loss cross-check would flag it
- **Gap:** Parse `geotag=lat,lon` into Photo, persist, show coordinates in the status-bar/viewer readout and a corner badge in the grid (badge machinery already exists for stars, grid.py:589-593). Preservation is free (read-only app).

### ❌ missing (M) Edit recipes: crop=, filters=, redo=, textactive=, text= — §4: 'ini wins for app state (stars, albums, edit recipes)'; M1 says 'full ingest'; display-with-edits fidelity

- **Evidence:** parse_rect64/parse_filters exist and are validated (picasa_db.py:686-712, 772-787) but the app ingests none of these keys (catalog.py:301-321 reads only star/caption/keywords/rotate/hidden/albums); grid/viewer render un-cropped, un-filtered pixels with only rotate= applied (grid.py:197-199, viewer.py:88-171)
- **Gap:** Two distinct pieces. (a) Ingest+preserve the recipe keys into the catalog (S — parsers exist; preservation is otherwise free since the app never writes inis). (b) Display fidelity: Picasa itself rendered unsaved recipes applied, so an M1 'see your library again' shows a cropped photo un-cropped today — applying crop64 (+tilt?) to thumbs/viewer is M; full filter rendering is the M3 edit engine and should NOT be pulled into M1. Needs an explicit product decision on where M1 display fidelity stops; recommend at least crop, since crop64 decode is already validated.
- **Notes:** Sized M for ingest + crop-applied display; full recipe rendering is M3/XL and out of M1 scope.

### 🟡 partial (S) Bookkeeping/rare keys: moddate=, originhash=, backuphash= (+ dynamic '<set name>-backuphash='), IIDLIST_<account>_lh=, width=/height=, screensaver=, [photoid], [(null)]

- **Evidence:** Parser preserves them all as opaque key/values (picasa_db.py:628-678) and parse_moddate exists (picasa_db.py:753-769); the app reads none (catalog.py:301-321); [(null)] tolerated by construction
- **Gap:** No M1 display need for any of these; a read-only app preserves them trivially by never writing inis, so §4 'Read: everything' is satisfied at the parser layer but not the app layer. Two have future read value worth a bead: moddate= could seed the Recently Updated auto-collection (v1 IN, post-M1), and width=/height= are free dimensions for RAW/video entries (M1 has RAW/video indexing). [photoid] and screensaver= are safely ignorable.
- **Notes:** moddate= semantics are still 'inferred — verify against oracle' per the format doc (line 57); an oracle fixture should precede any product use.

### 🟡 partial (S) Ini freshness: catalog reflects current on-disk ini state on warm start (substrate for §3 cohabitation/alternation; N6 foreign-write half lands M4, but stale display is an M1 trust issue)

- **Evidence:** Warm start loads the persisted catalog without re-reading any ini (catalog.py:440-449 'the warm path never re-reads inis'); reconcile_walk diffs ONLY photo size/mtime (catalog.py:532-562) — .picasa.ini files are not statted, so a star/caption Picasa wrote between sessions never surfaces until a photo file itself changes or --rebuild
- **Gap:** Include ini file signatures (path,size,mtime per folder) in the persisted catalog and the reconcile drift check, so an externally modified sidecar triggers the existing rebuild-and-swap path. The full per-key merge is M4; detection is cheap and makes M1's read-only view honest under alternation.

### ❌ missing (M) M1 gate: 'picasa_db.py survey cross-check shows zero ingest loss on synthetic corpora' (§9 M1)

- **Evidence:** The survey side exists (picasa_db.py:1287-1356 counts starred/captioned/keyworded/geotagged/face_tags/album_memberships/contacts/edit_keys) but no code anywhere compares those counts to an app catalog; no such check in test_tracer.py or .github/workflows
- **Gap:** Build the cross-check harness (survey a synthetic corpus, load/scan the same tree through catalog.py, diff feature counts) and wire it into CI. Note it will honestly FAIL today for geotagged, face_tags, contacts, and edit_keys — which is exactly the point of the gate; those findings above are its work list.

---

## .pal album files + contacts.xml ingest (Picasa compat contract §4, M1 "full ingest" §9)

### ❌ missing (M) §4 'Read: everything… .pal album files' / §9 M1 'full ingest (ini, .pal, contacts.xml, db3 rescue, XMP)' — read Picasa-2-era .pal album files

- **Evidence:** Zero .pal parsing anywhere: repo-wide grep for '.pal' hits only scripts/oracle-diff.py:226 (TEXT_SUFFIXES treats .pal as diffable text in the fixture harness) and docs. scripts/picasa_db.py has no pal code (no match). apps/desktop-python/catalog.py:317-321 and 329-347 build albums exclusively from ini `albums=` tokens + `[.album:<uid>]` sections. Glob **/*.pal finds zero files in the repo — not even a fixture. Spec: docs/product-spec.md:364-366, 838.
- **Gap:** Implement a .pal reader and wire it into ingest. Format knowledge on hand: one 2007 blog sample (docs/research/sources/forensicir-2007-picasa-analysis.md:234-267, duplicated in superuser-151146-picasa-file-format.md:121-147): an XML-ish <picasa2album> doc containing dbid, albumid, and properties uid (32-hex), category (num), date (OLE real64), token (']album:<uid>'), name, plus a <files> list of member paths in '[C]\sam3\sam1.jpg' drive-letter form. So it carries album identity + explicit membership (and the file-list order plausibly encodes album order — unvalidated, no fixture). Work: (a) tolerant XML parse in picasa_db.py; (b) path translation from absolute [drive] paths to library-relative photos (prior art: PicasaDBReader does 'path translation', prior-art-parsers.md:52); (c) locate the source dir — .pal files live in the user profile Picasa2Albums dir, outside the library, and main.py:937-973 has no profile-dir flag at all; (d) merge as gap-filler under §4. Caveat: the sample is a single mangled blog transcription; Picasa 3.9 itself never writes .pal (wine-oracle.md:209, 222 'albums own no file; no .pal', product-spec.md:384-386), so real ground truth needs a Picasa ≤2-era library or wild samples — the 3.9 Wine oracle cannot generate one.
- **Notes:** Practical priority is arguably low (only libraries touched by Picasa 2 have .pal), but the spec pins it to M1 explicitly, twice. No beads issue tracks it (bd list: 4 issues, none related).

### ❌ missing (S) §4 / §9 M1: read contacts.xml (contact id → person name registry) — the parser itself

- **Evidence:** No XML/ElementTree parsing in scripts/picasa_db.py (only hits: a pmp type-name string at line 349 and a docstring mention at 723). Repo-wide, contacts.xml exists only as an oracle capture: fixtures/oracle/014-face-tag-new-person/after/contacts/contacts.xml:1-3. apps/desktop-python has zero matches for 'contacts' at all.
- **Gap:** Write the parser: format is trivial and a real oracle sample exists — <contacts><contact id="ca5c88ca60f42c0b" name="…" modified_time="ISO8601" local_contact="1"/></contacts> (fixture 014). Data carried: 16-hex contact id (the join key to ini `faces=` ids and `[Contacts2]` keys, and to db3 person-album `albumcontactids` per picasa-db3-validated.md:59-62) → display name + modified_time + local_contact flag. Prior art (picasa2digikam, prior-art-parsers.md:25-29,53) additionally documents email attributes on <contact>, not captured in repo docs — worth re-deriving during implementation. Also needs source-location handling: contacts.xml lives outside the library at %LocalAppData%\Google\Picasa2\Contacts\contacts.xml (docs/research/sources/picasaresources/name-tags-in-picasa.md:127-131); the app currently has no profile-dir/--contacts argument (main.py:937-973).
- **Notes:** Hours of work for parser + tests given the fixture; the location/discovery plumbing is shared with db3 rescue (same Picasa2 profile dir) and the .pal dir, so build it once.

### ❌ missing (M) §9 M1 'faces displayed' read-only substrate: ingest ini `faces=` regions + resolve names via [Contacts2] and contacts.xml merge

- **Evidence:** The tracer ingests no face data whatsoever: catalog.py's Photo dataclass has no faces field (catalog.py:70-110), scan_library parses star/caption/keywords/rotate/hidden/albums only (catalog.py:301-321), and grep for face|Contacts2|contacts across apps/desktop-python returns zero code hits. README admits it: apps/desktop-python/README.md:146 'No faces, no edits, no writes.' The parser substrate DOES exist in the validated layer: scripts/picasa_db.py:719-739 parse_faces() decodes `rect64(..),<id>;` with the zero-pad-to-16 join rule and UNKNOWN_CONTACT (:716), and the survey counts [Contacts2] entries (picasa_db.py:1317-1318, 1331-1340) — but nothing extracts an id→name mapping for consumers.
- **Gap:** App-side ingest: call parse_faces per photo section; harvest [Contacts2] id→name from every folder ini (format: `<16-hex id>=<Name>;;`, split on ';' field 0, ids inherited downward — picasa-ini-format.md:33); merge with contacts.xml names. Name-conflict rule is already researched: contacts.xml wins over per-folder [Contacts2] on conflicts (picasa-ini-format.md:92-93); handle orphaned ids (name nowhere) and UNKNOWN_CONTACT (unconfirmed suggestion → §5's 'suggested' state, product-spec.md:247-248). Persist in the JSON catalog (catalog.py:361+) and surface names in the UI. Note the M1/M4 split: M1 is read+display of existing face data only; the writable in-library people registry, manual tagging, and import-into-native-state are M4 (product-spec.md:863-865) — do not build the registry now, just the read/merge/display path.
- **Notes:** Ingest+merge is ~a day on the existing substrate; the display surface (people in sidebar, face badges/rects in viewer) is adjacent UI work likely audited under grid/viewer — combined they trend L. 'faces displayed' is a named M1 deliverable (product-spec.md:839).

### ❌ missing (L) §4 precedence merge for the overlapping stores: 'album membership appears in ini, .pal, and db3; face names in ini, contacts.xml, db3, and XMP… merges with this provisional precedence, to be hardened… before M1 exit… every conflict is surfaced in the import report, never silently resolved' (product-spec.md:367-375)

- **Evidence:** There is no merge layer and no import report: catalog.py reads exactly one store (ini) for albums, so nothing to merge against — and its one multi-source situation (duplicate [.album:uid] definitions across folder inis) is resolved silently first-wins with a comment admitting the choice is arbitrary and unobserved (catalog.py:329-334). No db3 ingest exists in the app (only a comment, catalog.py:228). Precedence anchors in spec: ini authoritative for album membership (product-spec.md:287-288, 372); in-file XMP wins tier-1 incl. faces-in-XMP (:371); db3 fills gaps only (:372).
- **Gap:** Three pieces: (1) the merge engine — album membership: ini wins, .pal and db3 fill gaps for photos/albums the ini layer lacks; face names: XMP tier-1 where present, then contacts.xml > [Contacts2] (picasa-ini-format.md:93), db3 albumcontactids as last resort; (2) the import report that surfaces every conflict (also required by §3 for unknown-album-UID placeholders, product-spec.md:290-293) — nothing like it exists; (3) a pinned decision on .pal's precedence rank — §4's precedence sentence (:371-373) ranks ini/in-file/db3 but never ranks .pal, so its slot (presumably gap-filler beside db3, both being profile-local mirrors of the same album data) is an open design point the spec requires 'pinned by M1 exit' (:838-839). The duplicate-[.album:uid] rule also needs an oracle fixture instead of first-wins.
- **Notes:** The engine is L; the .pal-rank pin itself is a small decision but needs recording in the spec. 'Hardened empirically against the oracle before M1 exit' means fixture sessions (fauxcasa-dcc is in_progress) should include divergent-store scenarios; without that this trends XL.

### ❌ missing (S) Test substrate for the M1 gate ('zero ingest loss on synthetic corpora', product-spec.md:842-844): synthetic .pal + contacts.xml coverage

- **Evidence:** Zero .pal files exist anywhere in the repo (Glob **/*.pal: none); the only contacts.xml is the 1-contact oracle capture (fixture 014). docs/research/test-image-datasets.md:23,56 claims make-synthetic-library.py is 'the only source' for '.picasa.ini/.pal/contacts.xml' sidecars, but scripts/make-synthetic-library.py contains no match for pal|contacts|xml at all — the doc overstates the generator.
- **Gap:** Extend make-synthetic-library.py to emit (a) a synthetic Picasa2Albums dir with .pal files (some agreeing with ini albums=, some divergent, some orphaned) and (b) a synthetic contacts.xml with ids matching/missing/conflicting against [Contacts2] sections it already… does not write either — faces= and [Contacts2] generation may also be absent and needed for the faces gate. Then commit small fixtures. Also fix the test-image-datasets.md claim or make it true.
- **Notes:** Both formats are simple text; hours. Without this, the M1 exit cross-check (`picasa_db.py survey` zero-loss) structurally cannot exercise the .pal/contacts.xml paths.

### ✅ done (S) §4: 'We do NOT write .pal files' (product-spec.md:384-386) — writes generally are M2+

- **Evidence:** Confirmed no write path exists anywhere (no .pal code at all); oracle fixture 003 + wine-oracle.md:209 establish Picasa 3.9 itself creates albums with no .pal, so read-only .pal support is purely legacy import. contacts.xml write-back likewise has no v1 home (§3: Fauxcasa's people registry is in-library, product-spec.md:255-256, an M4 deliverable :864).
- **Notes:** Listed to scope the area: everything write-side for .pal/contacts.xml is out of M1 scope by explicit spec language; only the read/merge side above is M1 debt.

---

## db3 rescue import (spec §4: db3 fills gaps only — the four N3 DB-only rescue classes)

### ✅ done (S) §4 'Read: everything … the db3 catalog (.pmp + thumbindex)' — parser substrate: read pmp tables/columns and thumbindex structurally

- **Evidence:** A:\dev\fauxcasa\scripts\picasa_db.py:73-82 (all 8 pmp field types 0x0–0x7), :192-290 (read_pmp, strict/lax, Chromium-hardened), :313-320 (read_table — any <table>_<column>.pmp), :440-492 (read_thumbindex), :495-520 (thumbindex_full_paths — record-number→path join), :536-574 (read_repository). Oracle-validated: scripts/test_picasa_db.py:961-1001 (oracle integration tests); docs/research/oracle-db3-survey.md:34-38 ('every structured file parses strictly clean' — 37 pmp columns across imagedata/albumdata/catdata, thumbindex, both .dat stores) and :216-223 (clean at 1000-photo scale).
- **Notes:** Columns readable today (validated): imagedata — width, height, avgcolor, originfast, filetype, facerect, facerectdata, caption, rotate, edited, revertable, originslow, backuphash, crop64, filters, flipped, redo, text, textactive, colorspace, onlinechecksum, edit_width, edit_height, plus fixture-014 additions personalbumid, tagdate; albumdata — name, token, uid, date, category, filename, hascollage, location, music, description, inisync, albumcontactids; catdata — name, state, catpri. Semantic decoders: OLE date (:110-130), FILETIME (:358-369), rect64/crop64 incl. the uint64 pmp form (:686-712, test :246), faces= (:719-739), album categories 0/2/0xffff (:91-94). Semantic gaps handled in later findings: no category-8 (people album) constant, no ]facealbum:/]ignoreface/]unknownface token helpers, no contacts.xml reader, no join of the thumbindex face-crop virtual records to their parent photo.

### ❌ missing (L) §4/§9 M1: 'full ingest (ini, .pal, contacts.xml, db3 rescue, XMP) under the §4 precedence' — any db3 data ingested into the tracer catalog at all

- **Evidence:** The ONLY db3/pmp/thumbindex mention in apps/desktop-python is a comment: A:\dev\fauxcasa\apps\desktop-python\catalog.py:228-230 (fixture-032 note). catalog.py:45 imports picasa_db but uses only read_picasa_ini (:182) and parse_rotate (:313); scan_library (:263-358) is ini-only. main.py:933-975: CLI args are library/--thumbs/--cache-root/--rebuild/screenshot flags — no db3 path, and main.py has zero matches for db3/pmp/thumbindex/contacts/faces. README.md:146 confirms 'No faces, no edits, no writes'.
- **Gap:** Build the rescue-import plumbing the four classes all ride on: (1) locate the machine-local db3 dir (default %LocalAppData%\Google\Picasa2\db3, plus an explicit --db3 flag; PicasaStarter-style relocation exists in the wild); (2) read thumbindex + needed pmp tables and join record numbers (thumbindex index == imagedata row, validated 29/29); (3) map db3's absolute machine paths (Z:\…, C:\…) onto the library root the tracer was pointed at — the library is routinely mounted at a different path, so this needs suffix/root remapping with a miss report; (4) merge into Catalog/Photo under §4 precedence and persist (CATALOG_VERSION bump, catalog.py:388). This is read-only substrate M1 explicitly requires ('db3 rescue' is named in the M1 ingest list, product-spec.md:837-838).
- **Notes:** Photo dataclass (catalog.py:74-92) currently has no fields for any rescue-class data (no faces, no sort key, no date/geo override). The persisted-catalog format and warm-start path (load_catalog :440-512) must carry whatever the rescue import adds.

### ❌ missing (XL) Rescue class 1 (N3, §4): ignored faces — read from db3 and display for M1

- **Evidence:** No code anywhere: grep for 'ignoreface' hits only research docs (docs/research/picasa-binary-notes.md:85, picasa-ini-format.md:39-40 — the ]ignoreface marker string exists in the Picasa binary). No oracle fixture drives the ignore action: fixtures/oracle/ 001–033 contain face-tag (013/014) and reset-faces (026) but no ignore. docs/research/sources/picasaresources/how-to-rebuild-database.md:25 documents the loss class. Tracer has no faces model at all (catalog.py:74-92; README.md:146).
- **Gap:** Encoding is UN-PINNED, so this needs research before code: (1) new oracle fixture — drive Picasa 3.9 to ignore a detected/suggested face, wait out the lazy flush, diff db3 (hypothesis: an albumdata row with token ]ignoreface, or membership via the virtual face rows' personalbumid — unconfirmed); (2) add the decoded semantics to picasa_db.py + picasa-db3-validated.md; (3) ingest into the catalog and give it an M1 read-only surface (the §5 People 'suggested → confirmed / ignored' display, product-spec.md:483-487). Blocked-on-fixture makes this design-first.
- **Notes:** Fixture 026 (reset-faces) shows the adjacent machinery (facerect reverting to the 0x1 sentinel) but never produced an ignore record. Note also the oracle's synthetic library so far yields facerect/facerectdata = constant '1' sentinel = 'scanned, no face' (oracle-db3-survey.md:60-64) — suggestion/ignore state has never been observed on disk.

### ❌ missing (XL) Rescue class 2 (N3, §4): manual sort order in folders AND albums — read from db3 and display as folder/album order for M1

- **Evidence:** No sort-order column exists in the binary's full schema string table — imagedata and albumdata field lists at docs/research/picasa-binary-notes.md:26-38 contain no order/position column. No oracle fixture reorders anything (fixtures/oracle/ 001–033). Tracer order today is pure walk order: catalog.py:169-171 (sorted rglob) and album members appended in walk order (catalog.py:349-356); no per-folder sort modes in main.py. Loss class documented at how-to-rebuild-database.md:27 ('The order of pictures in both folders and albums is only saved in the Picasa database').
- **Gap:** Encoding UN-PINNED and not even located: candidate homes (a pmp column that appears on first use — db3 is column-sparse per oracle-db3-survey.md:243-245; the albums_0.db container; some other .db) are unexplored. Needs: (1) oracle fixture — manually reorder photos in a folder AND in an album, capture past the lazy flush, diff db3 (two fixtures or one combined); (2) decode + document; (3) ingest as per-folder and per-album display order in the grid ('manual is one mode among them', product-spec.md:442-444; album 'persistent manual order', :478). Design/research-first.
- **Notes:** Album membership itself is already ingested from ini albums= tokens (catalog.py:317-321), so only the ORDER is the db3-rescue piece. If the order turns out to live in albums_0.db (a 'thumbs.db-container format' file, picasa-binary-notes.md:66-68), a new container parser is required — no reader for that format exists in the repo.

### ❌ missing (XL) Rescue class 3 (N3, §4): video date/geotag edits — read from db3 and display (video date + geotag badge) for M1

- **Evidence:** Doubly absent. (a) Substrate: the tracer indexes no videos at all — EXTS at catalog.py:48 is images-only ('.jpg .jpeg .png .gif .bmp .tif .tiff .webp'), though M1 requires video indexing/playback (product-spec.md:840-841). (b) Encoding: no oracle fixture edits a video's date or geotag (fixtures 001–033); the binary schema has imagedata creation/modified/updated date columns and a gpsversion sentinel + KML geotag string (picasa-binary-notes.md:27-28, :40-41, :100-101) but no gps column was ever observed (oracle-db3-survey.md:42-52). Loss class documented at how-to-rebuild-database.md:29-31.
- **Gap:** (1) Oracle fixture: add a video to the oracle library, edit its 'date taken' and geotag it in Picasa, diff db3 past the lazy flush to find which imagedata columns (creation/updated? a new gps column?) carry the override; (2) decode + document; (3) once the tracer indexes videos (separate area), ingest the db3 date/geotag as display-date override and geotag corner badge (§5 badges, product-spec.md:440-441). Research-first; the ingest itself is small once the column is known and videos exist in the catalog.
- **Notes:** Photo dataclass has no date field at all today (dates aren't ingested even for stills — catalog.py:74-92), so the 'date override' has nothing to override yet; thumbindex taken_filetime (picasa_db.py:388-392) is the natural db3 source for baseline dates and is already parsed.

### ❌ missing (M) Rescue class 4 (N3, §4): contacts-only face names — join contacts.xml + db3 person-album rows to face tags so names display in M1

- **Evidence:** Encoding IS fully pinned — the only class that is: fixture 014 (fixtures/oracle/014-face-tag-new-person/diff.md) shows contacts.xml format (<contact id name modified_time local_contact>), the albumdata person-album row (category=8, token ]facealbum:<row>, name = person name, albumcontactids uint64 = contact id), and the virtual imagedata face row (filetype 1001, crop64=facerect, personalbumid, tagdate) + its thumbindex entry; normative writeup docs/research/picasa-db3-validated.md:52-67, :111-126. But NO reader: no contacts.xml parser anywhere (grep 'contacts.xml' in scripts/ hits only a docstring, picasa_db.py:723); picasa_db.py defines only categories 0/2/0xffff (:91-94) — no category-8 constant or ]facealbum token handling; the tracer catalog has no faces/people at all (catalog.py:74-92; README.md:146 'No faces').
- **Gap:** (1) contacts.xml reader (trivial XML, format observed) — locate it machine-local next to db3 (#contacts\ per picasa-binary-notes.md:64); (2) extract person names from albumdata category-8 rows (name + albumcontactids) as the second rescue source; (3) merge with ini faces=/[Contacts2] under §4 precedence (in-file XMP > ini > db3/contacts.xml fills gaps — a face whose contact id resolves ONLY via contacts.xml/db3 is exactly this class); (4) a faces field on Photo + a display surface (people in search and a People/faces affordance are M1: 'faces displayed', 'search over … people', product-spec.md:839-840, :451-453). Parsers are S; the catalog model + display surface is the bulk.
- **Notes:** make-synthetic-library.py already generates contacts.xml + face regions (docs/research/test-image-datasets.md:56), so synthetic test data exists. Caveat from fixture 026: Reset Faces removes the ini faces= line but leaves the contact, person album, and virtual imagedata row behind — the merge must treat a db3 face row WITHOUT a matching ini faces= line as untagged, not as a rescue candidate, or it resurrects deleted tags.

### ❌ missing (M) §4 precedence machinery: 'db3 fills gaps only … every conflict is surfaced in the import report, never silently resolved', precedence 'pinned by M1 exit'

- **Evidence:** No merge or import-report code exists in apps/desktop-python (only the ini-vs-in-file caption/keywords override, catalog.py:13-23 and inmeta.py — which resolves silently, no report). product-spec.md:369-374.
- **Gap:** An import-report artifact (per-photo/per-album conflict list: ini vs db3 vs in-file disagreements, unresolvable db3 paths, contacts.xml-only names) produced by the rescue import and surfaceable in the UI or as a file. Depends on the ingest plumbing finding; on its own it is a day of work.
- **Notes:** The existing ini/in-file precedence (catalog.py module docstring) should be folded into the same report so M1 exit can 'pin' the whole precedence empirically as §4 demands.

### ❌ missing (M) M1 gate: 'picasa_db.py survey cross-check shows zero ingest loss on synthetic corpora'

- **Evidence:** The survey tool exists and is counts-only by construction (picasa_db.py:1359-1421, _survey_features :1228-1266, _survey_ini_tree :1287-1356), but nothing compares its counts against the tracer's ingested catalog; test_tracer.py has no such test (grep for db3/pmp/survey in apps/desktop-python: one comment only).
- **Gap:** A cross-check harness: run survey over the synthetic library + its oracle db3, run the tracer ingest over the same, assert feature counts match (starred, captioned, album memberships, face tags, contacts, and — once ingested — the four rescue classes). This is the acceptance mechanism for everything above; it can land incrementally per class.
- **Notes:** Survey already counts face_tags/face_tags_named/contacts (picasa_db.py:1331-1340, :1317-1319) and albumdata categories, so the db3 side of the comparison is largely free; the work is on the tracer-catalog side plus rescue-class counters as they get pinned.

---

## In-file XMP/IPTC/EXIF ingest beyond captions/keywords (apps/desktop-python/inmeta.py + catalog.py + display surfaces)

### 🟡 partial (S) §4 tier-1 baseline: in-file JPEG caption/keywords (XMP dc:description/dc:subject + IPTC 2:120/2:25) read, with in-file winning over ini

- **Evidence:** A:/dev/fauxcasa/apps/desktop-python/inmeta.py:235-263 (reader, XMP-over-IPTC per field); thumbcache.py:278 and 405-413 (precedence applied at index, truthy-only override); catalog.py:13-23 (adopt-mode exception documented); test_tracer.py:636-772 (XMP/IPTC/precedence/UTF-8/extended-length/fail-soft/persistence tests)
- **Gap:** Adopt mode (--thumbs, the path every large/100k library uses) binds an external cache WITHOUT running the indexer, so the in-file read never happens and the catalog stays ini-only (catalog.py:21-23, README.md:152-157) — real Picasa JPEG captions/keywords are invisible on exactly the libraries M1 targets. Needs a metadata-only ingest pass decoupled from thumbnail building (reuse read_jpeg_metadata; bytes no longer piggyback on thumb decode, so it is its own I/O pass).
- **Notes:** The parser itself is solid and well-tested for these two fields. Non-JPEG carriers are covered in a separate finding.

### ❌ missing (L) Faces-in-XMP ingest (Picasa 'Write faces to XMP' → XMP-mwg RegionInfo regions) — §4 precedence: 'in-file metadata wins for tier-1 data (captions, keywords, faces-in-XMP)'; §5 People v1 'import all existing face data (ini + contacts.xml + db3 + XMP)'; §9 M1 'faces displayed'

- **Evidence:** inmeta.py:14-18 explicitly declares faces out of tracer scope; catalog.py:25-27 same; Photo dataclass catalog.py:73-93 has no faces field; grep for mwg/RegionInfo/face across apps/desktop-python finds no reader or display; docs/research/wine-oracle.md:214-215 and picasa-ini-format.md:93-94 show 'Write faces to XMP' REMOVES faces= from the ini
- **Gap:** Parse mwg-rs RegionInfo (structured RDF — nested rdf:Description structs in both attribute and element form, stArea normalized coords) from the XMP packet already extracted; add a faces field to Photo + catalog persistence (CATALOG_VERSION bump, catalog.py:388); merge with ini faces= / contacts.xml (both also unread — adjacent ini/contacts area); display: region overlay in viewer and people terms in search (§5 search covers 'people'). Critical for M1's zero-ingest-loss gate: a library where the user ran XMP write-back has NO ini faces= left — XMP is the only surviving face-region source, so today those faces are 100% lost.
- **Notes:** picasa_db.py:715-720 already decodes the ini faces= grammar (rect64+contact id) — reusable for the merge, but the XMP-mwg reader does not exist anywhere in the repo. Ingest alone is ~M; M1 'faces displayed' plus precedence merge makes it L.

### ❌ missing (M) Geotag v1: read + display — thumbnail corner badge + coordinates readout (§3 per-photo state: 'geotag (v1: read, preserve, display — badge + coordinates readout)'; §5 'Thumbnail corner badges (star, reject, geotag)'); sources: EXIF GPS IFD in-file + ini geotag=lat,lon

- **Evidence:** Grep for gps/geo across apps/desktop-python matches nothing functional; grid.py:589-593 draws only the star badge; main.py:901-913 selected-photo readout shows rel/★/caption/keywords only; catalog.py:301-321 scan_library reads star/caption/keywords/rotate/hidden/albums — the ini geotag= key (picasa-ini-format.md:62) is never read; inmeta.py ignores the EXIF APP1 entirely (test_tracer.py:721-723 asserts EXIF APP1 → EMPTY)
- **Gap:** EXIF GPS IFD parse (GPSLatitude/Longitude rationals + N/S/E/W refs) plus the ini geotag= key; Photo.geotag field + catalog persistence bump; geotag corner badge in grid paint; coordinates readout in the status bar (and viewer). Authoring/map panel is explicitly v1.5 — out of scope; read+display is v1 and the read substrate is M1.
- **Notes:** Precedence between ini geotag= and EXIF GPS when both exist is unpinned — §4 says in-file wins for tier-1 standard metadata (geotags are tier-1 per the §3 table); needs an oracle check of which store Picasa's own geotag action writes.

### ❌ missing (M) EXIF capture date (DateTimeOriginal) read for date grouping / per-folder date sort (§5 'per-folder sort modes (date / name / size / manual)'; §3 tier-1 'date'; footgun 16: unbounded dates — scans predate 1903)

- **Evidence:** Nothing in the app reads EXIF dates: grep DateTimeOriginal hits only scripts/make-synthetic-library.py:76,103,197 (the generator WRITES them) and picasa_db.py:389 / picasa-db3-validated.md:75 (db3 'taken' column research). mtime IS captured (thumbcache.py:274, catalog.py:87-93) but used solely as the N6 cheap staleness signal (reconcile_walk catalog.py:532-562) — never for grouping or sorting. The grid groups by folder in walk (path-component) order only, no date anywhere in the display model (grid.py:210-236); grep 'sort|mtime' in main.py: zero matches
- **Gap:** Answer to the direct question: neither EXIF date nor mtime drives grouping — there is no date-based grouping or sorting at all. Needs: EXIF APP1 TIFF/IFD walk for 0x9003 DateTimeOriginal (fallback 0x0132 DateTime, then mtime), a Photo date field + persistence bump, and per-folder date sort / date group headers in the grid. Must handle unbounded/absent dates (footgun 16; the synthetic benchmark deliberately includes 5% no-EXIF strays, make-synthetic-library.py:175).
- **Notes:** The M0 benchmark library embeds EXIF-date distributions precisely so this is gate-testable at 100k; the read can piggyback on the same bytes-in-hand path as inmeta (thumbcache._index_one) for the normal build, but the adopt-mode gap (finding 1) applies here identically.

### ❌ missing (M) XMP Rating ingest mapped to stars (§3 star authority: stars 0–5 map losslessly to XMP Rating; 'Foreign Rating ... maps to stars'; §9 M1 'full ingest (… XMP)' + 'stars displayed')

- **Evidence:** inmeta.py:223-232 parses only dc:description/dc:subject — xmp:Rating never read; Photo.star is a bool (catalog.py:78), so the 0–5 count model has no home in the data model; grid.py:589-593 renders a binary star badge; main.py:907-908 binary ★ in the readout
- **Gap:** Read xmp:Rating from the already-parsed XMP packet (trivial by itself) — but honest sizing includes the model change it forces: star bool→int 0–5 through Photo, catalog persistence (version bump), badge rendering, and the Starred view (main.py:798-800, 856-859). The rescan 'offer as star change + log' flow is M2+ (writes/reconciliation) — out of scope; M1 needs read+display of pre-existing Ratings only. Legacy ini star=yes→1-star mapping is also unimplemented (star stays a bool).

### ❌ missing (M) §4 'Read: everything … in-file XMP/IPTC/EXIF' across the §5 stills matrix: non-JPEG XMP carriers (TIFF/PNG/WebP), ExtendedXMP (>64 KB packets), and .xmp sidecars for RAW/video

- **Evidence:** inmeta.py:84 requires a JPEG SOI — any non-JPEG returns EMPTY (asserted by test_tracer.py:711-714); inmeta.py:247-250 takes only the first standard XMP APP1 — no ExtendedXMP (HasExtendedXMP GUID) reassembly; catalog.py:48 EXTS includes tif/tiff/png/webp/gif/bmp but no metadata path exists for them; no .xmp sidecar handling anywhere
- **Gap:** Per-container XMP extraction (TIFF IFD tag 0x02BC, PNG iTXt XML:com.adobe.xmp, WebP chunk) and ExtendedXMP reassembly for JPEG. The ExtendedXMP gap will bite exactly when finding 2 lands: face regions + edit history are what push Picasa-era XMP past the 64 KB APP1 limit, and today the overflow is silently dropped (an N7-shaped silence). RAW/video .xmp sidecars can defensibly wait for the RAW/video formats work, but JPEG ExtendedXMP cannot wait past faces-in-XMP.
- **Notes:** Mitigation making this survivable short-term: Picasa itself writes non-JPEG captions/keywords to the ini (picasa-ini-format.md:48-49 — 'only for formats with no IPTC/XMP home'), so Picasa-authored libraries are mostly covered by the existing ini path; what is lost is foreign-authored (Lightroom-class) XMP in TIFF/PNG — the exact interop workflow §1 names as a field report.

### 🟡 partial (L) Parser strategy: does the spec's wrap-a-mature-library call (§5 P1 'built on a mature metadata library (exiv2/exiftool-class — wrap, don't hand-roll)') apply at M1 for reads, or does the hand-rolled walker suffice?

- **Evidence:** inmeta.py:13-19 states the tracer stance explicitly ('the product wraps a mature metadata library, spec §5 P1') and scopes hand-rolling to two flat dc fields; the walker is genuinely robust for that scope (fail-soft segment walk inmeta.py:80-114, extended-length IPTC inmeta.py:161-166, UTF-8/latin-1 fallback, 10 targeted tests test_tracer.py:636-723). Spec §5 P1's library language literally governs the WRITER (an M4 deliverable), not reads
- **Gap:** The hand-rolled reader sufficed exactly as long as the scope stayed at two flat fields. Every remaining M1 item in this audit — EXIF IFD walking (dates, GPS rationals), structured mwg-rs RDF (faces), xmp:Rating, non-JPEG containers, ExtendedXMP — multiplies hand-rolled format surface in precisely the corner-case territory the spec's wrap-don't-reimplement stance exists to avoid, and the M4 writer will need the mature library anyway. Concrete remaining work: make the read-stack decision now (exiv2 bindings, or batched ExifTool -stay_open — NOT subprocess-per-file, which would break the §7 ≥30 photos/s index budget the current bytes-in-hand piggyback meets, thumbcache.py:229-233 and 278), integrate it behind the existing InMeta-style facade, and validate against the oracle fixtures. Hand-rolling only EXIF date+GPS (each individually small) is a defensible stopgap; faces mwg-rs + non-JPEG containers is where the library clearly pays.
- **Notes:** Verdict: at M1 the wrap-a-library call does apply once faces-in-XMP enters scope — recommend deciding it before implementing finding 2 rather than after, since the mwg parser is the single largest hand-roll and the first thing a library gives for free. A pure-Python-deps constraint (the tracer's no-new-dependencies minimalism, inmeta.py:18) is a tracer choice, not a product decision, per the module's own docstring.

---

## faces + people display (read-only M1 slice)

### 🟡 partial (M) M1 ingest: parse ini `faces=` (rect64 region + 16-hex contact id per face) into the catalog, per the §9 'full ingest (ini...)' and §3 faces data model (region + person ref)

- **Evidence:** Parser substrate DONE and oracle-validated: A:\dev\fauxcasa\scripts\picasa_db.py:719-739 (parse_faces: zero-pads %llx ids, tolerates trailing ';'), :680-712 (parse_rect64), :715-716 (UNKNOWN_CONTACT sentinel), tests A:\dev\fauxcasa\scripts\test_picasa_db.py:256-267 and :1050 (round-trips oracle fixture 014). App side MISSING: A:\dev\fauxcasa\apps\desktop-python\catalog.py:73-92 (Photo dataclass has star/caption/keywords/rotate/hidden/albums — no faces field), :301-321 (per-photo ini key handling never reads faces=), :27 (docstring: 'faces-in-XMP ... not ingested'), README.md:146 ('No faces, no edits, no writes'). Grep for face/people/contact across apps/ returns only these comments.
- **Gap:** Add Photo.faces (list of (rect, contact_id) with a suggested/confirmed flag), read `faces=` in scan_library via the already-imported picasa_db.parse_faces, persist in the JSON catalog (CATALOG_VERSION bump, catalog.py:388), and load_catalog round-trip. The `bd`/survey cross-check side already counts face_tags (picasa_db.py:1331-1340), so the M1 'zero ingest loss' gate has a comparator once the app ingests.
- **Notes:** Cheapest first step in the area; unblocks every display finding below. Grammar gotchas already documented (picasa-ini-format.md:86-96): orphaned ids, coordinates relative to stored pixels.

### ❌ missing (M) M1 ingest: contact-name resolution — ini [Contacts2] (3.9, names) / legacy [Contacts] (≤3.8, no names), plus contacts.xml, with contacts.xml winning name conflicts (§4 'Read: everything', §5 People v1 'import ALL existing face data (ini + contacts.xml ...)')

- **Evidence:** No contacts.xml parser exists anywhere, not even in scripts: A:\dev\fauxcasa\scripts\picasa_db.py references contacts.xml only in docstrings (:722-725); its survey merely counts [Contacts2] items (:1317-1318) and does not classify legacy [Contacts]. App: catalog.py builds a full section map (:188-203) so [Contacts2] sections flow through the ini reader but are dropped unconsumed. Format shape is pinned: fixtures/oracle/014-face-tag-new-person/diff.md:11-17 (single <contacts><contact id/name/modified_time/local_contact/> element), picasa-ini-format.md:32-33 (ids inherited downward from ancestor folders' inis; both tables can coexist), :92-93 (conflicting names across folders — contacts.xml wins).
- **Gap:** Write the trivial contacts.xml reader (one XML element type); resolve face contact ids → display names with precedence contacts.xml > [Contacts2] > [Contacts]-era 'unnamed', honoring ancestor-folder inheritance of [Contacts2] entries. Design point: contacts.xml is machine-local (Picasa AppData 'contacts/' dir, not the library), so the importer needs a discovery/point-at-it affordance (Windows default %LocalAppData%\Google\Picasa2\contacts\).
- **Notes:** Parser itself is S; the discovery UX + downward-inheritance merge + tests make it M. Without names, ini-only ingest shows anonymous hex ids.

### ❌ missing (L) M1 ingest: db3 rescue for face data — person albums (albumdata category 8, token `]facealbum:<row>`, identity in albumcontactids), virtual face imagedata rows (filetype=1001 carrying facerect/personalbumid/tagdate), db3 face names (§9 'db3 rescue'; §4 'face names in ini, contacts.xml, db3, and XMP'; db3 fills gaps only)

- **Evidence:** Generic pmp/table readers exist and are validated (picasa_db.py:193-320, read_table/read family), and the exact face semantics are fully documented from oracle fixtures 014/026: A:\dev\fauxcasa\docs\research\picasa-db3-validated.md:52-67 (category 8 person album; do NOT apply Chromium's empty-uid garbage rule to people albums — identity lives in albumcontactids, byte-equal to the [Contacts2] key), :115-126 (virtual face row: filetype 1001, crop64==facerect==the ini rect64, personalbumid → person-album row, parent photo's facerect gets the rect). But no face-specific join/rescue code exists anywhere, and the tracer app never opens db3 at all (catalog.py imports picasa_db for ini only).
- **Gap:** Build the face slice of the db3 rescue importer: join imagedata filetype=1001 rows → personalbumid → albumdata(category 8, name, albumcontactids) → contact id, map virtual rows back to parent photos (thumbindex parent linkage, picasa-db3-validated.md:117-118), and merge as gap-fill-only per §4. Note people albums exist ONLY in db3 — never in ini — so without this M1 cannot display people albums at all; catalog.py's album ingest (:329-347) reads only ini [.album:] sections.
- **Notes:** Multi-day: pmp joins, thumbindex parent resolution, gap-fill merge semantics, plus synthetic db3 test fixtures. Fixture 026 shows Reset Faces leaves stale [Contacts2]/contacts.xml/db3 residue behind — the merge must treat absent ini faces= as authoritative untag, db3 as gap-fill only.

### ❌ missing (L) M1 ingest: faces-in-XMP — in-file face data wins tier-1 per §4 precedence ('in-file metadata wins for ... faces-in-XMP'); §5 People v1 imports XMP face data

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\inmeta.py:14-19 explicitly scopes to caption/keywords only: 'Faces-in-XMP, geotags, dates, and the full XMP graph are out of tracer scope (the product wraps a mature metadata library, spec §5 P1)'. No mwg-rs/region parsing anywhere. Criticality pinned by research: picasa-ini-format.md:93-94 — Picasa's 'Write faces to XMP' option REMOVES `faces=` from the ini, so for libraries that enabled it XMP is the only sidecar-visible face source; and wine-oracle.md:214 (fixture 013): the People-panel flow writes the person name to XMP dc:subject with NO face geometry on disk at all.
- **Gap:** Two ingest shapes: (a) XMP mwg-rs face Regions (region rects + names) — needs either extending the hand-rolled inmeta.py XMP walker or making the §5 P1 'wrap a mature metadata library' call the spec already directs; (b) the 013 flavor where a face-derived name lands in dc:subject as a keyword (already ingested as a keyword by inmeta.py, but not distinguishable as a person). Coordinate normalization vs stored pixels and EXIF-orientation interplay need tests.
- **Notes:** L assuming the existing walker is extended; if it forces the wrap-a-metadata-library architecture decision first, treat as XL. Without it, §4's 'in-file wins for faces-in-XMP' precedence cannot be pinned by M1 exit as §9 requires.

### ❌ missing (M) M1 exit: §4 face-name/face-data merge precedence pinned empirically (in-file XMP wins tier-1; ini wins app state; db3 gap-fill only; every conflict surfaced in the import report, never silently resolved)

- **Evidence:** No merge engine and no import report exist for faces (or anything): the only cross-source merge in the app is caption/keywords in-file-over-ini (catalog.py:13-23, inmeta.py:29-31). Spec: docs/product-spec.md:367-374 (face names appear in ini, contacts.xml, db3, and XMP; provisional precedence 'to be hardened empirically against the oracle before M1 exit'), :837-839.
- **Gap:** Once the four sources are readable (findings above), implement the face merge with conflict records, and at least a minimal import-report surface listing face-name conflicts (contacts.xml-wins rule, picasa-ini-format.md:93). Harden against oracle fixtures 013/014/026 plus new differential fixtures for the conflict cases (conflicting names across folders, orphaned ids, XMP-vs-ini divergence).
- **Notes:** M for the face slice given all sources parsed; the import-report surface itself is shared cross-area infrastructure and may be owned elsewhere.

### ❌ missing (M) M1 display: 'faces displayed' — a People surface in the sidebar (people / person-album entries with photo counts), read-only

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\main.py:787-834 (_build_sidebar): exactly All photos / Starred / Folders / Albums — no People node; _apply_view (:850-869) has no person view kind. README.md:24 confirms the sidebar inventory.
- **Gap:** Add a People sidebar section listing named people (from the merged contact registry) with member counts, clicking filters the grid to that person's photos — the same set_filter mechanism albums use (main.py:861-864). Person membership comes from ini faces= contact ids plus db3 person albums (finding 3).
- **Notes:** This is the minimum credible reading of M1 'faces displayed'; it mirrors Picasa's own People tree (fixture 026 diff.md notes the 'People (1)' tree entry). Straightforward once ingest lands.

### ❌ missing (M) M1 display: per-photo face display — region overlay in the viewer and/or face names in photo metadata readout ('faces displayed', §3 region + person ref)

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:226-255 (paintEvent draws image + caption bar only, no overlay hooks); main.py:901-906 (_photo_selected metadata line: rel path etc., no faces); grid.py badges are star/error only (README.md:23).
- **Gap:** Viewer: draw face rects + names over the displayed image. The coordinate math is the real work: rect64 fractions are relative to STORED pixels, `rotate=` does not transform them, and EXIF orientation handling is the consumer's job (picasa-ini-format.md:94-96) — but this app bakes EXIF orientation at decode and composes rotate= on top (README.md:96-101), so the overlay must apply the same composed transform to the rects. Status-bar face-name readout is the cheap half.
- **Notes:** The transform-composition tests (all 8 EXIF orientations × rotate=) are most of the day. Grid face-count badges are NOT owed: §5 names thumbnail badges as star/reject/geotag only (product-spec.md:440-441).

### 🟡 partial (S) M1 search: 'instant as-you-type search over filenames, captions, keywords, people, folder names' (§5 View; M1 includes 'search')

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\main.py:880-887: search matches p.name, p.caption, p.keywords only — no people (and no folder names, noted for the search-area auditor). Spec: docs/product-spec.md:451-452.
- **Gap:** Add person names to the search predicate once Photo carries resolved face names. Trivial after ingest (findings 1-2).
- **Notes:** Caveat: fixture-013-style libraries put person names in dc:subject keywords, so those specific names are ALREADY searchable via the keyword path — but that is accidental coverage, not the people field.

### 🟡 partial (S) M1 read-only state semantics: suggested vs confirmed distinction imported from Picasa data (§3: 'suggested/confirmed/ignored states — in v1 the suggested state is populated solely by imported Picasa data'; §5: 'v1 suggestions come from import only')

- **Evidence:** The sentinel decode is done at parser level: picasa_db.py:715-716 (UNKNOWN_CONTACT = ffffffffffffffff = 'face detected, name suggestion unconfirmed'), :725-727 (faces with no suggestion are absent from ini entirely); survey already splits named vs unknown (:1338-1340). Nothing in the app consumes it (catalog.py Photo has no faces at all).
- **Gap:** Carry the suggested (UNKNOWN_CONTACT) vs confirmed (named contact) flag through catalog ingest and reflect it in the People/viewer display (e.g. suggested faces grouped under the unnamed affordance, not under a person). The 'ignored' state and the state-machine transitions are writes — M2+/M4, out of M1 scope.
- **Notes:** Rides finding 1's ingest; listed separately because it is the read-only half of the §3 state machine and easy to drop on the floor.

### ❌ missing (S) M1 honest-surface (read-only parts of §5 People v1): explicit unnamed-faces affordance and a visible count of photos not yet face-scanned (N7 spirit — 'the pre-recognition gap is never silent')

- **Evidence:** No such surface exists (main.py sidebar/status per findings 6-7). The data source for 'not yet scanned' is pinned: docs/research/oracle-db3-survey.md:60-64 — db3 imagedata facerect/facerectdata hold constant 1/'1' as a 'scanned, no face found' marker vs default 0 = not scanned; unnamed faces come from UNKNOWN_CONTACT entries and db3 ]unknownface stock album (picasa-db3-validated.md:63-65).
- **Gap:** After db3 rescue (finding 3): an 'Unnamed faces' entry in the People section plus a not-yet-scanned count in the People surface/status bar. Both are pure derived displays.
- **Notes:** Milestone ambiguity flagged honestly: full People v1 lands at M4 ('manual face tagging + people registry + face-data import', product-spec.md:864), so a strict M1 reading owes only 'faces displayed'; but both affordances are write-free and are what makes the M1 face display honest (N7), so recommend pulling them into M1. Depends on finding 3 for the scanned-marker data.

### ❌ missing (XL) Out of M1 scope (writes — M2/M4): manual face tagging (region + name), Fauxcasa-native people registry persisted in-library, suggested→confirmed/ignored state transitions, per-folder face opt-out, one-action 'remove all face data', people-album creation/editing

- **Evidence:** product-spec.md:481-487 (People v1 feature list), :846-849 (M2 = writes), :864 (M4: 'manual face tagging + people registry + face-data import'), :255-256 (§3 person registry in-library, not machine-local contacts.xml). Nothing implemented anywhere, consistent with milestone order.
- **Gap:** Not audited — M2+/M4 by the spec's own sequencing. Flagged only so the read-only findings above are not mistaken for the whole People v1 surface. One M1-relevant design shadow: the catalog's face/person data shapes (findings 1-3) should anticipate the M4 in-library person registry so read-only ingest doesn't bake in contacts.xml-keyed identity.
- **Notes:** Size reflects the M4 work if/when scheduled (people registry file format needs design first), not an M1 obligation.

### ❌ missing (S) M1 substrate: face-bearing test fixtures and app-level tests (M1 gate: 'picasa_db.py survey cross-check shows zero ingest loss on synthetic corpora')

- **Evidence:** The synthetic library generator writes no faces=/[Contacts2] at all (grep over A:\dev\fauxcasa\scripts\make-synthetic-library.py: zero matches); apps/desktop-python/test_tracer.py has zero face tests (grep). Parse-level coverage exists only in scripts/test_picasa_db.py:256-267, :928, :1050. Oracle fixtures 013/014/026 exist with decoded diffs but are not wired into any app test.
- **Gap:** Extend the synthetic generator to emit faces= lines, [Contacts2] sections, a contacts.xml, and a face-bearing db3 (or reuse the committed oracle 014 after/ tree as a fixture library); add tracer tests asserting catalog face ingest and the survey cross-check parity for face_tags/contacts counts.
- **Notes:** Cheap and load-bearing: without synthetic face data the M1 zero-ingest-loss gate cannot exercise the faces class in CI. Privacy rule already satisfied — oracle fixtures are synthetic and committed.

---

## grid & browse UI completeness (spec §5 Library & navigation + §5 Maintenance first-run, audited against apps/desktop-python/grid.py, main.py, catalog.py)

### ✅ done (S) Continuous-scroll thumbnail grid across all groups (§5), never reading originals while scrolling (N4)

- **Evidence:** apps/desktop-python/grid.py:107 (GridView on QAbstractScrollArea), grid.py:209-244 (set_filter builds one continuous grouped layout of all visible photos), grid.py:281-297 (_relayout stacks groups into one scroll range), grid.py:537-615 (virtualized paintEvent, visible+prefetch band only), grid.py:317-395 (decode workers read only the packed fcache, never originals), main.py:865-868 (sidebar folder click keeps the continuous all-photos view and scrolls to the folder rather than isolating it)
- **Notes:** Bounded decoded-tile LRU by bytes (grid.py:67-68, 443-464); §7 scroll budgets exercised by bench_scroll.py. Album/star/search views are separate filtered sets, which matches Picasa semantics.

### ✅ done (S) Pinned (sticky) group headers (§5)

- **Evidence:** apps/desktop-python/grid.py:495-511 (_sticky computes the pinned group + push-up offset as the next header slides in), grid.py:601-606 (in-flow headers then the pinned copy drawn last), grid.py:513-517 (photo_at treats clicks on the pinned band as no-hit), grid.py:262-268 (zoom anchor probes past the pinned band)
- **Notes:** No unit test covers _sticky or the pinned draw (grep for _sticky/pinned in test_tracer.py: zero hits) — works by inspection and screenshots only; a regression test would be cheap insurance.

### ✅ done (S) Live counts everywhere (§5): group headers, sidebar (All/Starred/folders/albums), status bar

- **Evidence:** apps/desktop-python/grid.py:621 (per-group header count), main.py:795-834 (All photos / ★ Starred / per-folder / per-album counts in sidebar), main.py:893-899 (_show_counts status aggregate: N photos · F folders · A albums), main.py:766-785 + 649-663 (sidebar rebuilt with fresh counts on reveal toggle and reconcile reload); test_tracer.py:1514-1524, 1544, 1589-1598 assert count text updates
- **Notes:** Counts are 'live' at read-only fidelity: they refresh on reveal toggle and on the background reconcile's atomic catalog swap. Once writes/watching exist (M2/M4) incremental count updates will be needed, but that is out of M1 scope.

### ✅ done (S) Thumbnail zoom slider (§5)

- **Evidence:** apps/desktop-python/main.py:469-482 (toolbar QSlider 64-256, 150 ms debounce), grid.py:253-276 (set_zoom clamps, re-anchors the viewport on the same content), grid.py:41-71 + 269-272 + 573-583 (tiles held at native ~256 px and scaled in paint, so zoom never re-decodes — fauxcasa-z1e)
- **Notes:** Max tile is capped at 256 px because the grid only reads the fcache's 256 level; larger Picasa-style zoom steps need the v2 512 level consumer (tracked as fauxcasa-q7m, README.md:124-132). Acceptable for M1 but worth a bead if larger grid zoom is wanted.

### 🟡 partial (S) Conventional scrollbar PLUS Picasa's jump-to-folder/end buttons (§5; recentering thumb deliberately dropped)

- **Evidence:** Conventional scrollbar: apps/desktop-python/grid.py:107, 293-296 (QAbstractScrollArea vertical bar, range/pageStep/singleStep). Jump substrate exists but no buttons: grid.py:305-309 (scroll_to_folder), grid.py:311-313 (scroll_to_fraction, used only by the sidebar and the --scroll-to CLI flag, main.py:1100-1103)
- **Gap:** Add the jump-to-previous-folder / jump-to-next-folder / jump-to-end button cluster to the UI (Picasa put them beside the scrollbar). All the navigation primitives (group list with y offsets, scroll_to_folder, scroll_to_fraction) already exist — this is a small overlay-widget + wiring job plus a boundary-stepping helper (current group from verticalScrollBar().value() via the existing bisect in _visible_groups).

### ✅ done (S) Thumbnail corner badge: star, in every grid context (§5)

- **Evidence:** apps/desktop-python/grid.py:84-89 (_star_polygon), grid.py:589-594 (gold star drawn top-right, size scales with tile: max(7, tile/14)); star ingested from ini star=yes at catalog.py:303 and persisted (catalog.py:393-394); one GridView renders every view (all/folder/album/starred/search) so the badge appears in all grid contexts
- **Notes:** Star is boolean (ini zero-vs-nonzero layer) — correct for M1 read; the 0-5 stack count arrives with M2 write machinery.

### ❌ missing (M) Thumbnail corner badge: geotag (§5; §3 geotag v1 = read, preserve, display — badge + coordinates readout). Reject badge is M2, out of scope.

- **Evidence:** No geo data anywhere in the app: Photo dataclass has no geo field (catalog.py:74-93); catalog.py:27-28 docstring states 'faces-in-XMP, geotags, and in-file dates are not ingested'; inmeta.py reads only XMP dc:description/dc:subject + IPTC 2:120/2:25 (inmeta.py whole file); grid paintEvent draws only star/selection/veil (grid.py:584-598). scripts/picasa_db.py knows the ini geotag key only as survey vocabulary (picasa_db.py:923-924, 1329-1330)
- **Gap:** M1 needs the read-only substrate end-to-end: (1) ingest EXIF GPS from originals at index time (extend inmeta.py — the bytes are already in hand for hashing) plus the ini geotag= key for video/legacy entries; (2) persist lat/lon in the catalog rows (bump CATALOG_VERSION); (3) paint a geo badge in grid paint; (4) coordinates readout in the status-bar photo-metadata mode. Reject badge deliberately not audited (M2).
- **Notes:** EXIF GPS parsing is fiddly (rational triplets, hemisphere refs) — wrapping a real metadata lib (spec §5 P1 'wrap, don't hand-roll') may be the moment to introduce it, which would grow this toward L.

### 🟡 partial (L) Flat + tree folder views (§5/§3: tree shows volumes; flat shows path on demand; both are pure views)

- **Evidence:** Tree view exists: apps/desktop-python/main.py:803-823 (node_for builds a nested QTreeWidget mirroring on-disk structure, N1). No flat view, no flat/tree toggle anywhere in main.py (grep 'flat' in main.py: no UI hits). No volumes: the library model is a single root directory (main.py:986 _resolve_library; catalog.py:119-124 Catalog has one root) — no multi-root/watched-roots, so nothing for a tree to show as volume nodes. Status bar shows a selected photo's library-relative path (main.py:905-906) but there is no flat-folder-view path-on-demand affordance
- **Gap:** Two distinct pieces: (a) flat folder view + toggle + path-on-demand (tooltip/secondary line per §3 'flat view must surface the on-disk path on demand') — mechanical within the current model, roughly a day; (b) 'tree shows volumes' requires the §3 multi-root library model (library-home + N watched roots, volume-UUID binding) which the tracer does not have at all — that is a library-model design/build item, not a sidebar tweak.
- **Notes:** Size L covers (a)+(b) minimally for M1 display purposes (multiple roots shown as top-level nodes); the full volume-UUID remount machinery is M4/N6 territory and should not be pulled into this item.

### ❌ missing (L) Per-folder sort modes: date / name / size (§5; manual is a fourth mode)

- **Evidence:** Display order is fixed at walk order (path-component name sort): catalog.py:164-174 (walk_library sorted(Path)), grid.py:209-244 (set_filter preserves catalog order within each group). No sort UI, no per-folder sort state, no re-sort path. Date sort is blocked on ingestion: in-file/EXIF dates are not read (catalog.py:27-28); Photo carries mtime/size only after indexing (catalog.py:88-93, thumbcache.py:404). No folder context menu exists at all (main.py sidebar wiring: itemClicked only, main.py:763)
- **Gap:** Needs: (1) EXIF DateTimeOriginal ingestion at index time + catalog persistence (same indexer piggyback as captions); (2) per-folder sort-mode state (machine-local view preference is defensible for read-only M1); (3) a sort-mode UI (folder context menu or toolbar dropdown, Picasa had both — picasa-ui-inventory.md:136,147); (4) group-items re-sort in set_filter/_apply_view without breaking the catalog-index→fcache-index mapping (items already carry catalog indices, grid.py:97, so re-ordering group.items is safe).
- **Notes:** Name (current implicit) and size (Photo.size exists post-index) are cheap once the mode plumbing exists; date ingestion is the long pole. Where the sort preference durably lives (tier-2 vs cache) should get a quick ruling before M2.

### ❌ missing (XL) Manual sort order DISPLAYED in M1, rescued from db3 (§5 'manual sort display for M1 from db3 rescue'; §3/N3: manual order is one of the four DB-only rebuild-loss classes)

- **Evidence:** The app reads no db3 at all: catalog.py imports picasa_db solely for ini parsing (catalog.py:45, 177-185); nothing references pmp/thumbindex in apps/desktop-python. Worse, the manual-order column is not yet reverse-engineered: docs/research/picasa-db3-validated.md contains zero matches for 'sort' or 'order' (grep confirmed); oracle-db3-survey.md:27-28 lists imagedata's 23 columns but no decoded ordering column
- **Gap:** Two-stage: (1) format research — identify and oracle-validate which db3 column(s) carry per-folder/per-album manual order (likely needs a new differential fixture: manually re-order a folder in the Wine oracle, diff the pmp files); (2) build the rescue-import path into the catalog (locate library's db3 dir, join thumbindex paths to catalog rels, apply order as the 'manual' sort mode's data). Nothing in the current code starts either stage.
- **Notes:** This is the only §5 grid item whose byte-level format is still unknown — it needs an oracle fixture before implementation can even be sized honestly. Recommend filing a bead for the fixture now; it also feeds the §4 'db3 fills gaps only' precedence hardening pinned to M1 exit.

### 🟡 partial (M) Status bar dual mode: collection aggregate / single-photo metadata (§5)

- **Evidence:** Both surfaces exist: apps/desktop-python/main.py:506-513 (counts_label + permanent meta_label on the QStatusBar), main.py:893-899 (aggregate: 'label: N photos · F folders · A albums'), main.py:901-913 (_photo_selected: rel path, ★, caption, #keywords), wired from grid selection and viewer (main.py:516-519); tests assert the aggregate text (test_tracer.py:1514-1524, 1589-1598)
- **Gap:** Single-photo mode is thin vs Picasa's: no date, no pixel dimensions, no file size, no geotag readout. File size is already in Photo.size (catalog.py:91) and dimensions are in the fcache entry table (thumbcache) or readable at index — surfacing those is hours; date and geo readouts are blocked on the ingestion gaps above. Also 'collection aggregate' currently means the whole current view — a multi-photo selection aggregate is impossible because the grid is single-select only (grid.py:125 selected: int; grid.py:688-693), which the selection-tray work will have to fix anyway.
- **Notes:** Recommend treating date/geo readout as riders on the geotag/date ingestion items rather than separate work.

### 🟡 partial (M) First-run = two questions max (scan scope; nothing else); library usable immediately (§5 Maintenance, M1-relevant)

- **Evidence:** One question only: frozen first-run shows a single folder picker (main.py:208-257 _prompt_for_library/_choose_library_from_dialog, main.py:288-327 _resolve_library), remembered for next launch (main.py:170-188); no other prompts exist. Usable immediately during indexing: the window opens before the thumbnail build, which streams live tiles into the grid (main.py:536-569 _start_cold_build cb → grid.feed_tile, grid.py:187-207), progress inline in the status bar (main.py:915-916) — modes, not modals. BUT the initial walk + ini parse is synchronous BEFORE the window exists: main.py:1021-1022 (scan_library on the cold path) runs before QApplication/MainWindow construction at main.py:1049-1056 — measured ~1.9-2.1 s at 100k local (README.md:33,49), unbounded on a slow/NAS volume
- **Gap:** Move the cold-start walk off the startup critical path so the window appears instantly and folders/photos populate as the walk discovers them (§5: 'library browsable during initial scan'). Today only the thumbnail/hash indexing is backgrounded; the walk itself blocks first paint, which on a NAS-scale first run reads as a hung app with no window.
- **Notes:** Question count is comfortably under the two-question max (the picker IS the scan-scope question). The --min/--max-image-size scan filters exist only as CLI flags (main.py:951-958) — fine, since exposing them at first-run would violate 'nothing else'.

---

## search + auto-collections (M1 audit)

### ✅ done (S) Instant as-you-type search over filenames (spec §5 line 451)

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\main.py:461-466 (QLineEdit wired to textChanged, live per-keystroke), main.py:880-887 (`text in p.name.lower()`); reveal-aware via `p.visible or self.grid.reveal` (main.py:882); tested in test_tracer.py:1641-1665
- **Notes:** Matches the leaf filename only (p.name), not the relative path — path matching is covered under the folder-names finding. Search is re-run on every keystroke with no debounce, which is what 'instant as-you-type' wants provided the §7 budget is met (see latency finding).

### ✅ done (S) Search over captions (spec §5 line 451)

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\main.py:884 (`p.caption and text in p.caption.lower()`); caption ingest from ini + in-file XMP/IPTC with §4 precedence: catalog.py:304, catalog.py:13-19 header comment, inmeta.py
- **Notes:** Caption source is the merged ini + in-file (XMP dc:description / IPTC 2:120) value once indexed; adopt-mode (--thumbs) catalogs stay ini-only (README.md:154-157) — a known, documented ingest caveat rather than a search gap.

### ✅ done (S) Search over keywords (spec §5 line 451)

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\main.py:885 (`any(text in k.lower() for k in p.keywords)`); keyword ingest catalog.py:305-307, in-file XMP dc:subject / IPTC 2:25 via inmeta.py
- **Notes:** Substring match against each keyword individually, so partial-keyword matches work.

### ❌ missing (L) Search over people (spec §5 lines 451-452)

- **Evidence:** Photo model has no people/faces field (catalog.py:74-91); catalog.py:27 states explicitly 'faces-in-XMP ... are not ingested'; grep for people/faces/contacts across main.py and catalog.py returns only that comment; README.md:146 'No faces'; search predicate (main.py:880-887) has no people clause
- **Gap:** Requires the people ingest substrate M1 needs anyway (§9 M1: 'full ingest (ini, .pal, contacts.xml, db3 rescue, XMP)' and 'faces displayed'): parse contacts.xml (no parser exists — scripts/picasa_db.py has parse_faces for ini faces= regions at picasa_db.py:719 and the [Contacts2] id convention at picasa_db.py:715-723, but no contacts.xml reader), ingest faces= per photo into the catalog, resolve contact ids to display names, then add a people clause to the search predicate. The search clause itself is trivial once names are on photos.
- **Notes:** This is a read-only M1 requirement, not deferrable to M2 — §9 M1 lists faces displayed and contacts.xml ingest. The ini-side region parser is already validated in scripts/picasa_db.py; the multi-day part is contacts.xml + [Contacts2] + XMP-face ingest and name resolution wiring into catalog.py.

### ❌ missing (S) Search over folder names (spec §5 lines 451-452)

- **Evidence:** Search predicate (main.py:880-887) tests only p.name, p.caption, p.keywords; p.folder (catalog.py:76) and Folder.title (catalog.py:112, set at catalog.py:288-297) exist in the model but are never consulted; the search box placeholder itself admits the scope: 'search filename, caption, keywords…' (main.py:462-463)
- **Gap:** Add a folder clause to the predicate — e.g. `text in p.folder.lower()` (matches any path segment) or match catalog.folders[p.folder].title. Data is already in memory; hours of work including a test.
- **Notes:** Decide whether a folder-name hit should show the folder's photos grouped (Picasa behavior) or just include them in the flat result set — the current single flat filter (grid.set_filter, grid.py:209) supports the latter with no extra work.

### ❌ missing (S) Negation (`-term`) in search (spec §5 line 452)

- **Evidence:** main.py:874 lowercases and strips the whole query and uses it as ONE literal substring (main.py:880-887); a query of '-term' is matched literally against names/captions/keywords; there is no tokenizer, no AND-of-terms, no negation handling anywhere in main.py
- **Gap:** Implement a minimal query grammar: split on whitespace, AND the positive terms, exclude photos matching any '-'-prefixed term. Note the current code doesn't even do multi-word AND (a two-word query is matched as one substring including the space), so tokenization + negation land together. Small, self-contained change to _search_changed plus tests.
- **Notes:** Spec line 453-454 notes Picasa users used a negation-search hack to fake All Photos — Fauxcasa has a native All Photos view, but negation itself is still an explicit v1 IN-list item.

### 🟡 partial (L) Search results are a selectable, bulk-operable set (spec §5 lines 452-453); selection tray is in §9 M1 scope (line 840)

- **Evidence:** Results feed grid.set_filter (main.py:888, grid.py:209) so they are a real navigable view; but selection is a single int — grid.py:125 `self.selected = -1  # catalog index`, grid.py:688-692 _select(idx), photo_selected is Signal(int) (grid.py:110). No multi-select, no ctrl/shift-click set semantics, no Hold/Clear tray anywhere in the app.
- **Gap:** Bulk OPERATIONS are M2+ (writes) — out of scope. But the read-only substrate M1 needs is missing: a multi-photo selection model over the filtered result set and the persistent cross-folder selection tray with Hold/Clear and type-aware readout (§5 lines 448-450, §9 M1 'selection tray'). That means replacing the single-int selection with a set, click/keyboard set-selection semantics, and the tray UI.
- **Notes:** The selection tray may also be counted in a sibling audit area; flagged here because the spec sentence in scope explicitly makes search results 'a selectable, bulk-operable set' and today they are only singly selectable.

### ✅ done (S) Native All Photos view (spec §5 lines 453-454)

- **Evidence:** Sidebar 'All photos' item with live count: main.py:795-797; _apply_view kind 'all' clears the filter (main.py:865-869); it is the startup default view (main.py:513) and the fallback for vanished views (main.py:735-743); reveal-aware count via _shown_count (main.py:702-706)
- **Notes:** Exactly the thing Picasa users needed the negation hack for; count updates across the Show-hidden toggle and reconcile reloads (main.py:662).

### ❌ missing (M) §7 budget: search keystroke → filtered grid < 50 ms at 100k (spec line 762; 'each row becomes a CI gate at the milestone where its feature lands', lines 752-753 — search lands in M1)

- **Evidence:** No timing instrumentation around _search_changed (main.py:873-889) — the only perf_counter timings in main.py are cold-start/prep (main.py:1010, 1042, 1080-1086); bench_scroll.py contains zero search coverage (grep 'search' → no hits); no search gate in .github/workflows (grep across *.yml → no hits); README.md:139-140 admits 'Search is a linear scan (fast enough ≤100k), not the per-photo word index the spec names' — an UNMEASURED claim
- **Gap:** (1) Measure: instrument keystroke→set_filter-complete latency and add a benchmark against the 100k synthetic library (the M0 generator exists), gated in CI like the scroll budget. (2) Likely optimize: the predicate re-lowercases every name/caption/keyword on every keystroke across all 100k photos in pure Python — plausibly over 50 ms; a precomputed lowercase haystack per photo (or the spec's word index, spec line 285 names a 'search index' cache component) is the probable fix. Also note grid.set_filter cost at 100k results is part of the measured path.
- **Notes:** Measurement alone is S; the M sizing assumes the linear scan misses budget at 100k and needs a precomputed haystack or index plus the CI gate. This is the biggest verification hole in the area: every other search claim is testable by reading code, but the headline budget has zero evidence either way.

### 🟡 partial (M) Starred Photos auto-collection with SCOPED views per footgun 17 (spec §5 lines 445-446; footgun 17 at line 739: 'Scoped star views + easy bulk-unstar')

- **Evidence:** A single global '★ Starred' sidebar view exists with a live reveal-aware count: main.py:798-801 (item + count), main.py:856-860 (_apply_view builds the starred index list and filters the grid); star ingested from ini presence-only key (catalog.py:303) and persisted (catalog.py:392-394); star badges render in the grid (grid.py:8, 595). No scoping of any kind — the view is all-stars-ever, exactly the footgun-17 accretion failure mode (84 stale stars).
- **Gap:** Scoped star views are the M1-relevant half of the footgun-17 mitigation (bulk-UNSTAR is an M2 write — out of scope). Missing: any scoping dimension on the Starred view — the spec never defines what 'scoped' means (by date range? per-folder? per-year?), so the semantics need a small owner/design decision, then the view mechanics (sub-items or a scope control, scoped counts, tests) are about a day. Also note star is a bool; 0-5 stacked stars are M2, and 'legacy star=yes imports as 1 star' (spec line 466) is satisfied in spirit by the boolean for read-only M1.
- **Notes:** Sized M for the mechanics, but flagged: the scoping semantics are undefined in the spec (line 739 says only 'Scoped star views'), so a definition must be pinned before implementation — if that counts as design-first, treat as XL-lite. The existing _apply_view starred path is the natural place to add a scope filter.

### ❌ missing (S) Recently Updated auto-collection (spec §5 line 446 — M1; Exports and Rejected are M2+, out of scope)

- **Evidence:** Zero occurrences of 'recent' in main.py or catalog.py (grep confirmed); sidebar builds only All / Starred / Folders / Albums (main.py:787-834); README.md:24 sidebar row lists the same four
- **Gap:** Add a 'Recently Updated' sidebar auto-collection. The read substrate mostly exists: Photo.mtime is captured and persisted (catalog.py:91, catalog.py:407-408, reload at catalog.py:479). Work: define the window/semantics for read-only M1 (Picasa's collection tracks recently edited photos; with no writes yet, file mtime recency is the honest proxy — worth a one-line decision), add the sidebar item + an _apply_view branch producing an mtime-sorted/filtered index list, counts, reveal-awareness, and a test. Caveat: adopt-mode (--thumbs) catalogs carry no per-file mtime signals (README.md:136-138), so the collection would be empty/degraded there — needs an honest empty-state.
- **Notes:** Structurally identical to the existing Starred view (a computed index list into grid.set_filter), so it's hours once the recency definition is picked.

### 🟡 partial (S) Sidebar/collection counts live everywhere for these views (spec §5 line 438 'live counts everywhere' as it touches search/collections)

- **Evidence:** Counts exist and are reveal-aware at build time: All (main.py:795-796), Starred (main.py:798-800), folders (main.py:818-822), albums (main.py:829-832), status-bar counts per view/search (main.py:860, 889, 893-899); sidebar counts refresh on reveal toggle and reconcile via _rebuild_sidebar (main.py:722, 661)
- **Gap:** Counts are recomputed only on sidebar rebuild events (reveal toggle, reconcile reload), which is sufficient while the app is read-only — they will need live invalidation when M2 writes land (starring a photo must tick the Starred count). No M1 action beyond awareness; recorded so the M2 auditor knows the counts are snapshot-built, not model-bound.
- **Notes:** Included as substrate context; primary audit of grid counts belongs to the grid/sidebar area.

---

## selection tray + keyboard triage (read-only slice)

### ❌ missing (L) §5: Selection tray — persistent CROSS-FOLDER selection with Hold/Clear (the tray UI itself is M1)

- **Evidence:** No tray widget exists: A:\dev\fauxcasa\apps\desktop-python\main.py:490-503 builds the browser page as sidebar+grid only (QSplitter, QStackedWidget); repo-wide grep for tray/Hold/Clear finds nothing in apps/desktop-python. Selection is a single catalog index: A:\dev\fauxcasa\apps\desktop-python\grid.py:125 (self.selected = -1), and it is DROPPED on any view change — grid.py:241-242 clears selection when the new filter's display set doesn't contain it, the exact opposite of cross-folder persistence.
- **Gap:** Build the tray: a persistent selection-set model (catalog indices, surviving set_filter/search/sidebar navigation and reconcile reload remaps), a tray strip widget rendering held thumbnails from the fcache, Hold and Clear actions, and add/remove wiring from grid clicks. Must also survive reload_data (main.py:649-663) which currently re-points everything and would orphan held indices — indices are positional per catalog, so held items need re-keying by rel path on catalog swap.
- **Notes:** The fcache/decoded-tile machinery in grid.py is reusable for tray thumbnails. Reconcile catalog-swap re-keying is the subtle part; everything else is conventional Qt work.

### ❌ missing (M) §5 (substrate for the tray): multi-photo selection in the grid — Ctrl/Shift-click, keyboard-extend — since a tray is meaningless over a single-select grid

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\grid.py:627-637 — mousePressEvent/mouseDoubleClickEvent ignore event.modifiers() entirely and call _select(idx) with one index; grid.py:688-693 _select stores one int; the photo_selected signal (grid.py:110) carries a single int (-1 = none); paint draws one selection rect (grid.py:595-598). No Ctrl+A, no Shift-range, no rubber band.
- **Gap:** Replace the single-index model with a selection set + anchor (for Shift ranges), modifier-aware mouse handling, multi-rect paint, a set-valued selection-changed signal, and keyboard range-extend (Shift+arrows). Downstream consumers (main.py:901-913 _photo_selected metadata label, main.py:925-929 _close_viewer) must handle set semantics.
- **Notes:** Prerequisite for the tray, the type-aware readout, and select/hold keys. test_tracer.py has zero selection/keyboard tests (grep for keyPress/Key_/photo_selected in tests: no hits), so this whole area is also untested.

### ❌ missing (S) §5: live type-aware selection readout — "Folder Selected — 14 photos"

- **Evidence:** The status bar shows VIEW counts, not selection: A:\dev\fauxcasa\apps\desktop-python\main.py:893-899 _show_counts prints "<view label>: N photos · F folders · A albums" for the active view, and main.py:901-913 _photo_selected shows single-photo metadata. Nothing tracks a selection TYPE (folder vs album vs photo-set vs mixed); sidebar clicks (main.py:836-848) change the grid filter but never form a 'Folder Selected' selection object.
- **Gap:** Introduce a typed selection concept (folder / album / N photos / mixed) fed by both sidebar selection and grid multi-select, and render the live readout string from it. Small once the selection-set model and tray exist; not implementable before them.
- **Notes:** The existing status-bar dual mode (collection aggregate at main.py:893-899 / single-photo metadata at main.py:901-913) is a genuine partial of the separate §5 'Status bar' item and gives the readout a natural home.

### ❌ missing (S) §5: tray as the universal input to every output action, with enablement keyed to selection type

- **Evidence:** No output actions exist (correct for M1 — they are M2+ writes/exports), and no action-enablement plumbing exists: the only QAction in the app is the Open-library action, A:\dev\fauxcasa\apps\desktop-python\main.py:457-459.
- **Gap:** M1's obligation is only the substrate: a selection model whose contents and type an action layer can query, plus an enablement hook. That falls out of the tray + typed-selection findings above; the output actions themselves are M2+ and out of scope here.
- **Notes:** Sized S on the assumption the tray (L) and typed selection (S) findings land first; on its own it has nothing to attach to.

### 🟡 partial (S) §5 keyboard: next/prev including J/K

- **Evidence:** Viewer: done — A:\dev\fauxcasa\apps\desktop-python\viewer.py:193-202 maps Left/K = prev, Right/J/Space = next, walking the grid's display order cross-folder (viewer.py:187-191), with Enter-to-open (grid.py:641-643) and Esc/Backspace-to-close (viewer.py:195-196) completing the loop. Grid: arrows only — grid.py:639-661 handles Left/Right (flat display order, crosses group seams) and Up/Down (group-aware column-preserving _row_step, grid.py:663-686) but has no J/K.
- **Gap:** Add J/K (and arguably Space) to the grid's keyPressEvent so the triage loop is key-consistent in both surfaces; trivially, J/K should route through whatever keymap layer the bindings finding introduces rather than more hard-coded key comparisons.
- **Notes:** Selection sync across surfaces already works: viewer.photo_shown updates the metadata label (main.py:519) and closing the viewer re-selects and scrolls to the viewed photo in the grid (main.py:925-930) — a real, working read-only triage skeleton.

### ❌ missing (S) §5 keyboard: select / hold key (add current photo to the held selection from the keyboard)

- **Evidence:** No key in either handler touches selection membership: A:\dev\fauxcasa\apps\desktop-python\grid.py:639-661 (arrows + Enter only) and viewer.py:193-202 (nav + close only). Picasa's Ctrl+H 'hold in tray' has no counterpart; grep for hold/tray in the app finds only unrelated comments.
- **Gap:** Bind a hold/toggle-select key (Picasa default Ctrl+H, plus plain toggle in a multi-select grid) in both grid and viewer, mutating the tray selection set. Hours of work once the tray + multi-select exist; blocked until they do.

### ❌ missing (M) §5 keyboard: 100% (1:1) zoom toggle in the viewer — the explicit N4 exception, loads async

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:207-224 _display_rect only ever aspect-FITS the image to the window (cap=True merely prevents upscaling past native); there is no zoom state, no 1:1 toggle key in keyPressEvent (viewer.py:193-202), and no panning. The toolbar zoom slider (main.py:469-482) is the separate thumbnail-size slider, not this.
- **Gap:** Add a 1:1 toggle (key + click-to-zoom), a pan model (drag / arrow-pan at 1:1 — note arrows currently mean next/prev, so 1:1 mode needs a key-priority decision), and paint from the already-async-loaded original. The async original load and instant cached preview (viewer.py:79-127) are done and reusable; this is view-transform + input work only.

### ❌ missing (M) §5 keyboard: hover full-screen peek (from the grid; async original load per N4)

- **Evidence:** No hover handling anywhere: A:\dev\fauxcasa\apps\desktop-python\grid.py has no mouseMoveEvent/hover/enter handlers (input section is grid.py:627-661, mouse press/double-click/keys only), no mouse tracking enabled, and no full-screen overlay widget exists in main.py or viewer.py.
- **Gap:** A hover-armed full-screen borderless peek surface: hover + trigger detection in the grid, a frameless fullscreen widget, instant fcache-preview paint with async original swap-in (both mechanisms exist in viewer.py:79-127 and can be shared), and dismissal on mouse-leave/key-up.
- **Notes:** N4 (spec §2) names peek as one of the two deliberate read-originals exceptions, so it is squarely M1, not polish.

### ❌ missing (S) §5 keyboard: reveal-in-file-manager (locate on disk)

- **Evidence:** No implementation: grep across apps/desktop-python for reveal/explorer/xdg-open/locate/file manager returns nothing relevant; neither keyPressEvent (grid.py:639, viewer.py:193) nor any action (main.py:457 is the only QAction) offers it. Full paths are available (viewer.py:87 builds catalog.root / photo.rel).
- **Gap:** One per-platform action + key binding (Picasa default Ctrl+Enter): Windows 'explorer /select,', macOS 'open -R', Linux best-effort (org.freedesktop.FileManager1 ShowItems, falling back to opening the folder). Hours, no design needed; §8 makes the per-platform variants v1 work.

### ❌ missing (S) §5 keyboard: platform-correct Ctrl/Cmd (§8: Ctrl/Cmd keymaps are an owned v1 cross-platform consequence)

- **Evidence:** Zero modifier-based shortcuts exist to BE platform-correct: no event.modifiers() check, no QKeySequence, no QShortcut anywhere in the app — grid.py:639-661 and viewer.py:193-202 compare bare event.key() values only; grep for ControlModifier/MetaModifier/QKeySequence in apps/desktop-python: no hits.
- **Gap:** When the binding layer lands, use QKeySequence standard keys / Qt's native Ctrl↔Cmd mapping rather than raw key comparisons, and verify on macOS conventions (blocked-on-hardware per §8, but the code side is cheap to get right now).
- **Notes:** Absorbed almost entirely by the keymap-layer finding below; listed separately because the spec names it explicitly.

### ❌ missing (M) §5 keyboard: Picasa-compatible default bindings as the default scheme (a coherent keymap, not scattered hard-coded keys)

- **Evidence:** All bindings are hard-coded inline in two keyPressEvent bodies: A:\dev\fauxcasa\apps\desktop-python\grid.py:639-661 and viewer.py:193-202. There is no keymap table, no named-action indirection, and no reference to Picasa's binding set; several Picasa read-only defaults (Ctrl+Enter locate, Ctrl+H hold, Ctrl+A select-all) are simply absent.
- **Gap:** Introduce a small action->QKeySequence default-scheme table (the 'Picasa-compatible scheme' the spec names) that grid/viewer/tray consume, seeded with the Picasa defaults for every read-only action in this audit. Needs a one-page enumeration of Picasa's actual bindings from the research corpus first, then mechanical.
- **Notes:** This is the natural single fix that closes the J/K, hold-key, Ctrl/Cmd, and locate-binding gaps together.

### ❌ missing (S) §5 keyboard: star keys (taps stack, 0–5 set keys) and reject/reverse-star (X)

- **Evidence:** No digit/X handling in grid.py:639-661 or viewer.py:193-202; stars are display-only throughout (grid badge grid.py:589-594, viewer bar viewer.py:250-251, sidebar count main.py:798-801).
- **Gap:** Out of M1 scope — these are M2 writes per the spec (§9 M2, §5 Organize) and the audit brief. The only M1-relevant slice is reserving their slots in the default keymap scheme (previous finding) so the M2 write actions drop in without rebinding.
- **Notes:** Noted for completeness per the brief, not audited as an M1 gap. Read-side star display substrate is done (badge, Starred view, viewer bar).

---

## viewer + slideshow (M1)

### ✅ done (S) Open single-photo viewer from the grid (double-click / Enter on selection)

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\grid.py:632-642 (mouseDoubleClickEvent + Return/Enter), grid.py:695-697 (_activate emits photo_activated with display list + pos); A:\dev\fauxcasa\apps\desktop-python\main.py:517 (wiring), main.py:920-923 (_open_viewer swaps QStackedWidget page, focuses viewer). Scriptable via --open N (main.py:965-967, 1104-1109).
- **Notes:** Solid. Viewer is a page in the same window (modes-not-modals, §1), not a dialog.

### ✅ done (S) Viewer navigation next/prev including J/K (§5 keyboard triage loop), walking the grid's current display order

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:187-202 (_step + keyPressEvent: Left/K back, Right/J/Space forward, clamped at ends), viewer.py:69-77 (show_photo takes the grid's display list + pos).
- **Notes:** Space currently means 'next'; when slideshow lands, Space conventionally becomes pause — a key-reassignment to remember, not a bug. Nav order is the grid's display order, so from a folder context it walks the whole continuous-scroll set (matches the Picasa continuous-grid model; per-folder scoping matters only for the slideshow, below).

### ✅ done (S) Close viewer / return to grid (Esc) with grid selection following the viewed photo

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:193-196 (Esc/Backspace), viewer.py:204-205 (double-click closes); A:\dev\fauxcasa\apps\desktop-python\main.py:925-930 (_close_viewer re-selects + scrolls the grid to the photo, restores focus).

### ✅ done (S) Instant cached preview on open/step: viewer paints a thumbnail-cache stand-in immediately, never a blank viewport (N4 spirit; fcache v2 loupe consumer)

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:89-97 (synchronous cheap stand-in), viewer.py:129-174 (_preview_min_edge picks a hi-DPI-aware level via ThumbCache.best_level()/entry(); broad-guard degrade-to-text on corrupt cache), viewer.py:207-224 (_display_rect makes the original→preview hand-off a sharpen-in-place). Tests: A:\dev\fauxcasa\apps\desktop-python\test_tracer.py:1175-1304 (v2 level selection, v1 fallback, no-cache, error-tile, supersede/fallback, rotate composition, display-rect math).
- **Notes:** Well tested. Known documented cosmetic caveat: an original smaller than the window pops down from the filled preview because the cache stores thumb dims, not original dims (viewer.py:214-218). README.md:131 still says '(the viewer still reads originals, N4)' — stale text from before fauxcasa-9pp landed; one-line doc fix.

### ✅ done (S) Async original load: full-resolution decode off-thread, UI never blocks on a slow volume; the viewer is the explicit N4 exception to 'never read originals'

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:99-127 (daemon-thread decode with stale-serial guards bounding the backlog under key-repeat; QImageReader.setAutoTransform for EXIF + Picasa rotate= composed on top), viewer.py:176-185 (_on_loaded: original supersedes preview; null decode keeps preview). Stale-drop test at test_tracer.py:1307+.
- **Notes:** No prefetch of the next photo — acceptable for the viewer (preview hides latency) but worth adding when the slideshow needs seamless advance.

### ❌ missing (M) 100% (1:1) zoom toggle in the viewer — the explicit deliberate exception named in N4 (§2) and the §5 keyboard triage loop ('100% zoom toggle')

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:193-202 — keyPressEvent handles only Esc/Backspace/Left/Right/J/K/Space; no zoom key, no click-to-zoom. viewer.py:207-224 (_display_rect) only aspect-fits (cap=True merely prevents upscaling past native — it is a fit clamp, not a 1:1 view). Grep for zoom/Key_Z/mouseMove in viewer.py: no matches.
- **Gap:** Add a fit↔1:1 toggle (key + click, Picasa muscle memory), rendering at native pixels with devicePixelRatio correctness, anchored at the click/center point, with panning (mouse drag; arrows currently mean next/prev so pan needs its own gesture), and correct interaction with the preview (preview upscales blurry at 1:1 until the original lands — acceptable, but state must carry across J/K steps or reset per photo, a small decision).
- **Notes:** A minimal center-anchored 1:1 without smooth panning is S; doing it properly (pan, anchor, DPR) is a solid day. This is an M1 item — N4's wording makes 1:1 zoom a named, deliberate part of the read-only performance identity.

### ❌ missing (M) Hover full-screen peek from the grid (§2 N4 'explicit 1:1 zoom and the hover full-screen peek are the deliberate exceptions and load asynchronously'; §5 keyboard triage loop)

- **Evidence:** Grep for mouseMoveEvent/setMouseTracking/hover in A:\dev\fauxcasa\apps\desktop-python\grid.py: no matches (grid handles only press/double-click/key input, grid.py:627-661). No peek surface anywhere in apps/desktop-python.
- **Gap:** A hover-triggered frameless full-screen preview: mouse tracking in the grid, a trigger decision (pure dwell vs modifier+hover — Picasa used Ctrl+Alt hover; needs a one-line design call), a borderless top-level widget reusing the viewer's cached-preview + async-original machinery (viewer.py:_load_preview and the loader thread are directly reusable), dismissal on mouse-out/keypress, multi-monitor placement.
- **Notes:** The hard parts (instant preview, async original, EXIF/rotate composition) already exist in ViewerPage — the new work is the hover state machine and the full-screen surface. Half a day to a day.

### ❌ missing (M) Slideshow: basic per-folder/album play button + full-screen timed playback loop (§5 In & out; §9 M1 names 'slideshow'; §10 item 11 settles playback as M1). Overlay star/reject controls are the M2 write half — out of scope here except as read-only chrome.

- **Evidence:** No slideshow code exists anywhere in the app: grep for slideshow/play/QTimer-advance across apps/desktop-python hits only bench_scroll.py's --fullscreen flag and viewer docstrings. viewer.py has no timer, no auto-advance, no full-screen mode; main.py's toolbar (main.py:454-488) has search/zoom/reveal only — no play button; the sidebar (main.py:787-834) has no per-folder/album play affordance.
- **Gap:** Build the M1 playback loop: (1) a play affordance scoped to the current folder/album view — folder scoping needs a folder-member index list (the album filter already yields one, main.py:861-864; folders currently apply no filter, main.py:865-869, so a small helper builds the folder's display subset); (2) a full-screen playback mode (showFullScreen on the viewer page or a dedicated top-level widget) with a QTimer advance using a default slide delay that waits for the async original (viewer's loader + stale-serial machinery is directly reusable; add next-photo prefetch for seamless advance); (3) minimal transport: pause/resume (Space), manual prev/next, Esc exits back to the grid at the current photo, end-of-set behavior (loop or exit — pick one); (4) read-only overlay chrome (position, pause/next/prev/exit) laid out so the M2 star/reject buttons slot in without redesign.
- **Notes:** Sized M (~a day): ViewerPage already supplies async load, instant preview, display-order navigation, and key handling — the genuinely new work is the full-screen surface, the timer/prefetch loop, the play button, and folder-scoped display sets. Not XL: Picasa precedent pins the design. Budget creep risk: transitions/pan-zoom belong to the M4 movie renderer, not this — keep the M1 slideshow to cut-advance.

### ✅ done (S) Viewer displays per-photo state read-only (star, caption) — the M1 substrate under the M2 overlay controls

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:248-258 (bottom bar: position n/N, rel path, ★ when starred, caption); main.py:901-913 mirrors the same into the status bar via photo_shown.
- **Notes:** Interactive star/reject in viewer and slideshow are M2 writes — out of scope. The reject flag isn't displayed anywhere yet, but reject itself is M2 (§5 Organize, §10 item 15), so no M1 gap.

### ❌ missing (L) Viewer renders what M1 ingests: §9 M1 includes 'stills + RAW rendering, video indexing/playback' — the viewer is the surface where RAW render and video playback must appear

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\catalog.py:48 — EXTS = {.jpg,.jpeg,.png,.gif,.bmp,.tif,.tiff,.webp}: no RAW, no video, no PSD/TGA, so such files never reach the viewer; A:\dev\fauxcasa\apps\desktop-python\viewer.py:109-119 decodes solely via QImageReader (stills only) — a video or RAW path would show 'could not decode this file' (viewer.py:243-246).
- **Gap:** RAW decode (LibRaw-class) and video playback (bundled ffmpeg-class) plumbed through the viewer, plus the §5 decode-isolation requirement. Flagged here because the viewer is the rendering surface; the format/decoder work itself is presumably the formats-area audit's to size in detail.
- **Notes:** Cross-area boundary: counting only the viewer-side plumbing this is M, but it cannot land without the decoder/indexing substrate (L, and decode isolation per §5 needs the M0-exit threat-model mechanism). Defer authoritative sizing to the formats auditor to avoid double-counting.

### ❌ missing (M) Filmstrip in the viewer/edit room

- **Evidence:** No filmstrip anywhere in viewer.py/main.py (viewer paints a single image + caption bar, viewer.py:226-259).
- **Gap:** None for M1 — the filmstrip is part of the M3 edit room (§5 Edit: 'Edit room one double-click from the grid: filmstrip, breadcrumb, histogram + EXIF panel'). Noted per audit instructions, not an M1 gap.
- **Notes:** Out of scope for M1; recorded so the milestone boundary is explicit. When M3 arrives the grid's fcache tile machinery makes a filmstrip roughly a day.

---

## Still-image format matrix + RAW (spec §5 Formats, M1 per §9 "stills + RAW rendering" and "Formats + decode isolation→M1")

### ✅ done (S) Stills scanning + rendering for JPEG, PNG, TIFF, GIF, BMP, WebP (6 of the 8 listed formats)

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\catalog.py:48 EXTS = {".jpg",".jpeg",".png",".gif",".bmp",".tif",".tiff",".webp"}; A:\dev\fauxcasa\scripts\make-thumbcache.py:74 identical set; decode is format-agnostic QImageReader in the indexer (thumbcache.py:286-300) and viewer (viewer.py:109-112); catalog dimension probe also QImageReader (catalog.py:130-143). Empirically verified on the pinned build: `uv run --with PySide6-Essentials==6.11.1` → QImageReader.supportedImageFormats() = bmp cur gif icns ico jfif jpeg jpg pbm pgm png ppm svg svgz tga tif tiff wbmp webp xbm xpm. The PyInstaller bundle preserves the imageformats/ plugin dir explicitly (fauxcasa-tracer.spec:31-35), so the frozen build keeps qjpeg/qgif/qtiff/qwebp.
- **Notes:** One residual: the test corpus is entirely Qt-generated JPEG (test_tracer.py:73-88 etc.) — no per-format decode fixture, so PNG/GIF/TIFF/WebP work by construction (same QImageReader call) but are never regression-tested. Adding one tiny fixture per format to the synthetic corpus is hours. Failure mode is already graceful: an undecodable file becomes a zero-length blob = error tile (thumbcache.py:256, 299-300), never a crash. Grid hot path is unaffected by source format — it decodes only app-written cache JPEGs (grid.py:363).

### ❌ missing (S) Stills: TGA support (§5 line 546)

- **Evidence:** ".tga" absent from both EXTS sets (catalog.py:48, scripts/make-thumbcache.py:74) — TGA files are never scanned. Yet the pinned PySide6 6.11.1 build DOES ship the qtga plugin (verified: 'tga' appears in QImageReader.supportedImageFormats()), so the decode path already works.
- **Gap:** Add ".tga" to both EXTS constants (they must change in lockstep — cache-order parity rule, catalog.py:47 and catalog.py:164-168), plus a fixture. Safe for existing caches: bind() detects the count/path mismatch and triggers rebuild (thumbcache.py:199-208). Also add a TGA fixture to the synthetic corpus. make-thumbcache.py's PIL path reads TGA natively.

### ❌ missing (M) Stills: PSD support (§5 line 546)

- **Evidence:** ".psd" absent from EXTS (catalog.py:48, make-thumbcache.py:74), AND Qt cannot decode it — 'psd' is not in QImageReader.supportedImageFormats() on the pinned PySide6 6.11.1 build (verified empirically). The in-app indexer (thumbcache.py:264-298) and viewer (viewer.py:109-112) are Qt-only, so adding the extension alone would yield error tiles.
- **Gap:** Needs a non-Qt decode branch: Pillow's PsdImagePlugin reads the flattened composite (the Picasa-equivalent behavior; only PSDs saved with 'maximize compatibility' carry one — others legitimately error-tile). Work: extension in both EXTS sets; a Pillow fallback in thumbcache._index_one and viewer._load_full when QImageReader returns null; Pillow added to the app deps + PyInstaller spec (currently the app has no Pillow dep — only the standalone make-thumbcache.py uses PIL); PSD fixture + test. Alternatively defer PSD behind the same pluggable-decoder seam the RAW work needs.

### ❌ missing (M) Per-extension include/exclude panel (§5 line 546-547; Picasa's Tools > Options > File Types)

- **Evidence:** EXTS is a hardcoded frozen module constant (catalog.py:47-48) mirrored in make-thumbcache.py:74; no settings UI exists anywhere in main.py (the only scan-scope controls are the CLI flags at main.py:951-958); no config surface for extensions in cache_dir_for/config handling.
- **Gap:** Build a File Types panel (checkbox per supported extension), persist the chosen set per-library, thread it into walk_library the way ScanFilter already is (catalog.py:164-174), and fold it into the cache key exactly like ScanFilter.cache_key does (catalog.py:65-70, consumed at main.py:1001) so each extension-set gets its own cache dir and warm starts stay coherent. Constraint: the walk rule is load-bearing for cache-order parity (catalog.py:4-11), so the extension set must be part of cache identity, and make-thumbcache.py needs a matching --exts flag for adopt-mode parity. The plumbing pattern (ScanFilter) already exists to copy.

### ✅ done (S) Min-image-size filter (Picasa's 'ignore small images' analog; area question: does it exist?)

- **Evidence:** --min-image-size and --max-image-size CLI flags (main.py:951-958, parsed main.py:330-342, wired main.py:990-993); ScanFilter dataclass (catalog.py:53-70); applied during the walk via header-only QImageReader size read (catalog.py:130-174 — unreadable headers are deliberately KEPT so the indexer surfaces an error tile, catalog.py:146-151); filter participates in cache identity via cache_key (catalog.py:65-70 → main.py:1001); tested (test_tracer.py:245-278).
- **Notes:** CLI-only — no settings-panel UI. Fine for the tracer; when the File Types panel (previous finding) is built, min/max size belongs on the same page (hours, not days). Also note the filter probes dimensions with QImageReader, so once RAW extensions are added, RAW headers Qt can't size will pass through unfiltered (acceptable: 'None means keep').

### ❌ missing (S) RAW: detect at least Picasa's 16-vendor extension list (§5 lines 548-549; source list = DNG, CRW, CR2, RAW, RAF, 3FR, DCR, KDC, MRW, NEF, NRW, ORF, RW2, PEF, X3F, ARW, SRF, SR2 — 18 unique extensions, docs/research/sources/picasaresources/files-supported-by-picasa3.md:87-119)

- **Evidence:** Zero RAW extensions in either EXTS set (catalog.py:48, make-thumbcache.py:74); repo-wide grep for rawpy/libraw/nef/cr2/arw/dng in apps/desktop-python finds nothing but incidental variable names; the only LibRaw mentions in the repo are the spec (docs/product-spec.md:550, 771) and the threat model (docs/decode-threat-model.md:16, 93).
- **Gap:** Add the 18 extensions to both EXTS sets (lockstep, cache-order parity). Detection alone is a few hours — but shipping it without the decode path (next finding) turns every RAW file into an error tile, so land them together. One trap to design around: several RAW containers are TIFF-based (NEF/CR2/ORF/PEF/ARW/DNG); QImageReader content-sniffs when the extension is unknown, so qtiff may 'succeed' on some of them and produce a garbled or embedded-preview image rather than failing cleanly — RAW extensions must be routed explicitly to the RAW decoder, never fall through to Qt.

### ❌ missing (L) RAW: auto-render "as if JPEG" (§5 lines 86, 548-552) via a maintained UPDATABLE LibRaw-class library — never a frozen in-binary table (§6 footgun 14, docs/product-spec.md:736); M1-scoped per §9:840 "stills + RAW rendering" and §9:882 "Formats + decode isolation→M1"

- **Evidence:** No RAW decode path anywhere: indexer decode is QImageReader-only (thumbcache.py:264-298), viewer full-load is QImageReader-only (viewer.py:105-125), standalone builder is PIL-only (make-thumbcache.py:128-139); rawpy/libraw appear nowhere in code, deps, or the PyInstaller spec (fauxcasa-tracer.spec:79-88, binaries=[], no rawpy hiddenimport). Qt's format list contains no RAW format (verified on pinned 6.11.1).
- **Gap:** Full RAW decode path via rawpy (the LibRaw Python binding — satisfies 'updatable' since LibRaw updates ship as a wheel bump, and the threat model already blesses LibRaw-class-in-sandbox, docs/decode-threat-model.md:93): (a) thumbnail path — branch in thumbcache._index_one for RAW extensions, preferring rawpy's embedded-thumb extraction (extract_thumb; most RAWs carry a full-size JPEG preview, so §7 index rate barely moves) with fallback to postprocess(half_size=True); orientation parity with setAutoTransform (LibRaw applies flip itself — needs a fixture assert); (b) viewer path — rawpy postprocess with camera white balance / sRGB / 8-bit = the 'as if JPEG' render, on the existing loader thread; (c) make-thumbcache.py parity branch (PIL cannot decode RAW) so adopt-mode caches stay order- and content-compatible; (d) bundling — add rawpy to the run deps and the PyInstaller spec; rawpy wheels bundle libraw (win/mac/linux), PyInstaller needs the DLL collected (hook or binaries entry), expect +~5 MB; (e) synthetic RAW fixtures (tiny DNGs can be generated; vendor samples must be redistribution-clean — committed fixtures must be synthetic per the privacy rule) + tests + a §7 index-rate spot-check on a RAW-heavy folder.
- **Notes:** Dependency to flag, not audited here: the M1 decode-isolation decision (docs/decode-threat-model.md:101-105) requires ALL original-file decode in sandboxed worker processes, while today decode runs on in-process threads (thumbcache.py:224-233, viewer.py loader thread). RAW decode should be built behind whatever worker-pool seam that work introduces rather than as another in-process QImageReader-style call — otherwise it gets rewritten immediately. If the sandbox seam lands first, this stays L; building rawpy in-process now and migrating later adds rework but doesn't need design (rawpy API is settled), so it does not rise to XL on its own.

---

## video indexing + playback (spec §5 Formats "Video" bullet; §9 M1 "video indexing/playback")

### ❌ missing (M) §5 Formats / §9 M1: videos are indexed — video files appear in the catalog and grid alongside stills

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\catalog.py:48 — EXTS = {".jpg", ".jpeg", ".png", ".gif", ".bmp", ".tif", ".tiff", ".webp"} (stills only); catalog.py:169-171 walk_library filters on that set, so video files are never walked; scripts/make-thumbcache.py:74 duplicates the same stills-only EXTS (the cache-order parity contract, catalog.py:47 and 164-168, requires both change in lockstep); a repo-wide grep for video/mp4/avi/mov/QMediaPlayer/ffmpeg in apps/desktop-python hits ONLY fauxcasa-tracer.spec:61-62 (the excludes list). Picasa's documented video extension list is in docs/research/sources/picasaresources/files-supported-by-picasa3.md:33-73 (.mpg .wmv .asf .avi .mov .3gp .mp4).
- **Gap:** Add the Picasa video extension set to catalog.py EXTS AND scripts/make-thumbcache.py EXTS together (walk-rule parity or caches stop binding); add a media-kind field to Photo (catalog.py:74-92 has no type/duration field) and bump CATALOG_VERSION (catalog.py:388) so stale catalogs cold-rebuild; wire video sidecar sections through — .picasa.ini sections for video files (star=, caption=, albums=) parse fine in picasa_db but are currently dropped because the walk never emits the file, which is ingest loss against the M1 gate's 'zero ingest loss' cross-check; tests in test_tracer.py (currently zero video coverage). Grid itself is index-driven and never reads originals, so it needs little beyond a poster tile (finding 2) — a video-type affordance/duration overlay is UX-desirable but not among the spec-mandated badges (star/reject/geotag).
- **Notes:** The extension-list edit alone is trivial; the M sizing is the parity contract, catalog schema change, ini-section attachment, and tests. Depends on finding 2 for a non-blank tile (without a poster extractor, indexed videos would render as error tiles — thumbcache length-0 entries — which at least degrades visibly, not silently).

### ❌ missing (M) §5/§9 M1: video thumbnail in the grid is a poster frame

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\thumbcache.py:264-299 — the index decode path is QImageReader-only (setScaledSize DCT-scaled JPEG decode); same in scripts/make-thumbcache.py. No frame-extraction code anywhere in the app. The bundling precedent exists only in repo tooling: scripts/fetch-videos.py:4-7 (PEP 723 dep on imageio-ffmpeg) and :37 (imageio_ffmpeg.get_ffmpeg_exe()) with ffmpeg-CLI frame extraction at :55-60.
- **Gap:** Poster-frame extraction at index time via a bundled decoder — the two candidate mechanisms are (a) imageio-ffmpeg subprocess per the fetch-videos.py precedent (works in both the in-app builder and scripts/make-thumbcache.py, no Qt packaging change) or (b) QtMultimedia frame grab (blocked by the current packaging, see finding 4). Wire into thumbcache.build_cache and make-thumbcache.py; reuse the existing error-tile fallback (length-0 entries, thumbcache.py:94) for undecodable videos; watch the §7 index-rate budget (one ffmpeg subprocess per video is much slower than a scaled JPEG decode — needs measurement on a mixed corpus).

### ❌ missing (L) §5/§9 M1: videos play back in the viewer

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\viewer.py:109-125 — ViewerPage decodes via QImageReader only (off-thread QImage); viewer.py:226-246 paints a QImage or the text 'could not decode this file' — a video opened today would show that error text. No QMediaPlayer/QVideoWidget/QVideoSink/QAudioOutput anywhere in the app (grep).
- **Gap:** A playback surface in ViewerPage: media-kind dispatch (needs finding 1's Photo field), QMediaPlayer + QVideoWidget/QAudioOutput (or an ffmpeg-frame-fed path if Qt Multimedia is rejected), minimal transport (play/pause, position), preserving the existing keyboard loop (←/→ J/K nav, Esc close, viewer.py:193-202) and the async never-block-UI discipline; poster frame as the pre-play stand-in (the existing preview path, viewer.py:137-174, works unchanged once the fcache holds a poster). Also intersects the §5 decode-isolation requirement (untrusted-input decode with no ambient authority, chosen at M0 exit) — the playback path must be built inside whatever isolation mechanism that area lands on, which is a reason not to defer this to a bolt-on.

### ❌ missing (M) §5 Formats: playback/index use BUNDLED decoders (ffmpeg-class), never OS codecs — the explicit Picasa anti-pattern ('if WMP or QuickTime couldn't play it, Picasa couldn't either', files-supported-by-picasa3.md:85); §8 requires the identical format matrix on every platform; footgun table §6 #14

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\main.py:4 — dependencies = ["PySide6"] (unpinned meta-package: dev runs DO get QtMultimedia via PySide6-Addons, and Qt ≥6.5's default QtMultimedia backend is its bundled ffmpeg). But the shipping artifact forecloses it: fauxcasa-tracer.spec:61-62 explicitly excludes PySide6.QtMultimedia and QtMultimediaWidgets, and .github/workflows/bundle.yml:35 pins the bundle build to PySide6-Essentials==6.11.1, which physically omits QtMultimedia (it ships in PySide6-Addons). No ffmpeg of any kind is bundled or depended on by the app (only scripts/fetch-videos.py, repo tooling).
- **Gap:** Choose and implement the decoder-shipping mechanism: (a) Qt route — add the QtMultimedia module + its bundled ffmpeg DLLs to the PyInstaller bundle (reverse spec excludes, move off Essentials-only or add the Addons wheel), and pin the backend (QT_MEDIA_BACKEND=ffmpeg) so Windows can never silently fall back to the native WMF backend — falling back would violate 'never OS codecs' silently, an N7-class failure; or (b) imageio-ffmpeg subprocess for indexing plus a non-Qt playback path. Then re-measure bundle size/cold start (§7 explicitly accepts installer growth for bundled decoders; cold start is the soul constraint) and update bundle.yml's smoke assertions.
- **Notes:** The spec-file excludes are documented as belt-and-suspenders self-documentation (fauxcasa-tracer.spec:29-30, 52-55), so reversing them is deliberate policy work, not an accident to patch. Design space is small and both precedents exist in-repo; not XL.

### ❌ missing (M) §3 per-photo state: 'video date/geotag overrides (v1: imported Picasa values preserved losslessly per N3)' — the M1 read half, via §9 M1 'full ingest (… db3 rescue …)'; these overrides are one of the four documented DB-only rescue classes (§2 N3). Authoring new overrides is M2+/post-v1 (out of scope here).

- **Evidence:** A:\dev\fauxcasa\apps\desktop-python\catalog.py:45,182,313 — the app imports picasa_db but uses only the ini surface (read_picasa_ini, parse_rotate); no db3/pmp ingest exists anywhere in apps/desktop-python. The validated parsers exist unwired in scripts/picasa_db.py (per §4 the db3 read path is 'the rescue importer for the four DB-only data classes').
- **Gap:** After finding 1 lands (videos must exist in the catalog to attach overrides to), ingest video date/geotag override rows from the db3 rescue read and carry them losslessly in the persisted catalog for display. Overlaps the general db3-rescue-ingest area — flagged here only for its video slice so it isn't dropped between the two areas.
- **Notes:** Sized for the video slice assuming the general db3-rescue plumbing is costed in the ingest area; if that area doesn't exist, this inherits its cost.

### ❌ missing (S) §9 M1 gate substrate: the gate corpora must exercise video — 'zero ingest loss on synthetic corpora' and the N4/§7 budgets on the 100k synthetic library can only gate video indexing if the corpora contain videos (M0's generator spec says 'mixed formats')

- **Evidence:** A:\dev\fauxcasa\scripts\make-synthetic-library.py:162-169 — BENCH_FORMATS is JPEG/PNG/GIF/BMP/TIFF/WEBP only, no video; find over fixtures/ returns zero video files; test_tracer.py has zero video-related tests (grep across apps/desktop-python).
- **Gap:** Add a small deterministic video component to make-synthetic-library.py (tiny mp4/avi clips generatable via the imageio-ffmpeg precedent) including .picasa.ini sections referencing them (star/caption/albums) so the ingest-loss cross-check and index-rate budget cover video; add synthetic video fixtures + tests for catalog/thumb/viewer video paths.

---

## decode isolation / threat model

### 🟡 partial (S) §5 Formats: isolation mechanism 'chosen with the stack (fauxcasa-6hf) against a written threat model, at M0 exit — before M1 work begins'

- **Evidence:** docs/decode-threat-model.md:1-9 (M0-exit deliverable, bead fauxcasa-bdj CLOSED, commit 75eca81); mechanism decided at docs/decode-threat-model.md:97-121 ('A is the floor, B is the trajectory, C is never sufficient alone'); bead fauxcasa-6hf CLOSED with owner direction 'proceed with Python+Qt', notes name 'wasm = decode sandbox (docs/decode-threat-model.md)'
- **Gap:** Doc header still reads 'Decision state: proposed — agent draft for owner review... overturnable by argument' (docs/decode-threat-model.md:7-9). Owner review rode fauxcasa-6hf; 6hf is closed but its close reason ratifies the stack, not the isolation mechanism explicitly. Record an explicit owner confirm-or-argue on the mechanism and flip the header to decided.
- **Notes:** Substantively the M0-exit gate was met: threat model exists (assets, attacker model, trust boundaries, mechanism comparison, residual risks, CI hooks) and a mechanism was chosen before tracer M1 work. Only the ratification bookkeeping is open.

### ❌ missing (XL) §5 Formats (spec docs/product-spec.md:557-562) + M1 list (docs/product-spec.md:838-842, 'decode isolation per the §5 threat-model requirement'; gate map 882-883 'Formats + decode isolation→M1'): decode of untrusted input runs with no ambient authority. Threat model commits to mechanism A: 'All decoding and metadata parsing of original files happens in a sandboxed worker pool, on all three platforms, from M1... The broker process opens files and passes fds/buffers; workers cannot open anything themselves' (docs/decode-threat-model.md:101-105), no exceptions for 'simple' formats

- **Evidence:** All untrusted-original decode is in-process with full app authority: apps/desktop-python/thumbcache.py:233 (INDEX_WORKERS = in-process thread pool), thumbcache.py:264-298 (QImageReader on original bytes inside the app process), thumbcache.py:272 (worker itself does src.read_bytes() — no broker); apps/desktop-python/viewer.py:109-113,125 (full-res QImageReader(path).read() of the original in an in-process daemon thread); apps/desktop-python/catalog.py:134-138 (QImageReader(str(path)).size() header parse of untrusted files during scan filter). Repo-wide grep for AppContainer/seccomp/bubblewrap/Landlock/wasmtime/wasmer/sandbox-exec matches only docs/decode-threat-model.md; the only subprocess use in the app tree is test scaffolding (test_tracer.py:2263+). apps/desktop-python/README.md never lists this among its 'Deliberate tracer shortcuts' (README.md:115-161) — an omission in the honest-status ledger.
- **Gap:** Build the entire mechanism-A decode service: (1) broker/worker-pool architecture — broker opens files and passes fd/shm blobs, workers return fixed-shape length-checked pixel buffers over shm, trusted side validates dimensions (doc:64-67, 146-148); (2) per-platform authority stripping — Linux namespaces+seccomp/Landlock, Windows AppContainer/restricted-token+job-object, macOS App Sandbox/sandbox-exec (doc:93); (3) migrate all three call sites (index decode+hash+thumb-encode, viewer full-res load, catalog header-size probe) into the pool, with thumbnail JPEG re-encode happening INSIDE the sandbox so the cache's trusted-by-provenance argument (doc:69-73) holds; (4) per-job kill/timeout supervision. The threat model chose the mechanism but the concrete IPC protocol, worker lifecycle, and per-platform sandbox profiles have no written design. No tracking bead exists (bd search 'decode isolation'/'sandbox'/'threat model' → no open issues) — the largest unstarted M1 item is currently untracked.
- **Notes:** Doc explicitly forbids format exceptions: 'No exceptions for simple formats — JPEG parsers have the richest exploit history of all' (doc:104-105). The mechanism is deliberately stack-independent (doc:117-121), so Python+Qt host is no excuse. Availability-only crashes are out of threat-model scope (doc:50-51) but crash isolation comes free with the pool (doc:93).

### ❌ missing (M) Threat model trust boundary: 'The metadata parser (exiv2-class) sits inside the same boundary as pixel decoders' — original file bytes including XMP/IPTC/EXIF/MakerNotes are always untrusted (docs/decode-threat-model.md:60-64); decision item 1 includes 'metadata parsing' in the sandboxed pool (doc:101)

- **Evidence:** apps/desktop-python/inmeta.py:36 (xml.etree.ElementTree parse of untrusted XMP) invoked in-process from the index worker at apps/desktop-python/thumbcache.py:278 (read_jpeg_metadata(data) on raw untrusted bytes), running inside the app process thread pool (thumbcache.py:233)
- **Gap:** Move in-file metadata parsing (inmeta.py, and later the 'mature metadata library' the product wraps per §5 P1) into the same sandboxed worker pool. The current parser is pure Python (memory-safe, fail-soft per inmeta.py:20-22) which lowers memory-corruption risk, but the doc rules that memory-safety is 'never a substitute' for the sandbox (doc:95, 114-115), and ElementTree remains exposed to XML pathologies with full app authority today.
- **Notes:** Size is the call-site migration once the finding-2 pool exists; it has no standalone path. The piggyback design (bytes already in hand for hashing, thumbcache.py:250-254) transfers naturally: the worker that decodes pixels also parses metadata inside the boundary.

### 🟡 partial (S) Threat-model decision item 2: 'The UI/index process decodes only app-written cache artifacts (thumbnails, preview tiles)' — the N4 hot path stays outside the sandbox by provenance, not by exception (docs/decode-threat-model.md:106-108, 69-73)

- **Evidence:** Conforming half: the grid never decodes originals — apps/desktop-python/grid.py:363 (QImage.fromData on fcache blobs only; README.md:23 'Never reads originals (N4)'), and the viewer's instant preview decodes only cache levels under broad guards (viewer.py:137-159). Violating half: the same UI/index process also decodes untrusted originals (thumbcache.py:286 index, viewer.py:111 full-res, catalog.py:138 header probe).
- **Gap:** The 'only' clause becomes true automatically when finding 2 lands — no independent implementation work. One provenance caveat to close by design: adopt mode (--thumbs, README.md:117-120, 154-156) binds an externally supplied pre-built fcache and the UI decodes its blobs; 'trusted by provenance' holds only for caches the app itself built. Either verify/attest adopted caches or document that adopting a cache asserts trust in its builder.
- **Notes:** The grid-reads-only-the-cache architecture is real and load-bearing — it is exactly what the threat model calls 'the architecture the stack trial balloons benchmarked' (doc:72-73). The good bones exist; the sandbox around original decode does not.

### ❌ missing (L) Threat-model M1 CI gate: 'a test worker, handed a hostile-format probe corpus, demonstrably cannot (a) open a file outside its handed-in fd, (b) reach the network, (c) write anywhere but its output shm — asserted per platform' (docs/decode-threat-model.md:125-128)

- **Evidence:** No isolation tests exist (test_tracer.py has no sandbox/escape assertions; repo grep for sandbox mechanisms matches only the doc). CI runs on ubuntu-latest + windows-latest only (.github/workflows/tracer.yml:27, tests.yml:16-18) — no macOS runner anywhere, while the doc requires 'all three platforms' (doc:102) and 'asserted per platform' (doc:128).
- **Gap:** Build the hostile-format probe corpus; write per-platform escape-assertion tests (file-open outside handed fd, network reach, writes outside output shm); wire them as a blocking CI gate on Linux, Windows, AND macOS — which also means adding macOS CI, absent from every workflow today.
- **Notes:** Explicitly labeled 'M1 gate (with decode isolation landing)' in the doc — it ships with finding 2, not after it. The kill -9 crash-robustness check (doc:129-130) ties to the N5 kill-fuzzer, which the spec gates at M2 (product-spec.md:882), so that piece is out of M1 scope.

### ❌ missing (L) Threat-model fuzz smoke: 'the decoder corpus (vendor RAW samples, truncated/malformed files from the format research) runs through the pool under the sandbox in CI; any worker escape attempt fails the build' (docs/decode-threat-model.md:131-133)

- **Evidence:** No fuzz corpus or fuzz CI job exists (.github/workflows/ contains balloons.yml, tests.yml, bundle.yml, tracer.yml — none run a decoder corpus); no pool exists to run it through (finding 2)
- **Gap:** Assemble the malformed/truncated decoder corpus (format research references exist under docs/research/), run it through the sandboxed pool in CI with escape detection failing the build. Depends on findings 2 and 5; the corpus assembly itself can start independently.
- **Notes:** Doc section heading is 'Verification (becomes CI gates)' (doc:123) — these are commitments, not aspirations. Vendor RAW samples imply this matures alongside RAW support, but truncated/malformed still-image probes are buildable now from synthetic fixtures.

### ❌ missing (M) Updatable decoder bundle: 'the pool interface versions the decoder bundle so security updates ship without app releases' (docs/decode-threat-model.md:134-136, §6 footgun 14)

- **Evidence:** Decoders today are Qt's image plugins frozen into the PyInstaller bundle (.github/workflows/bundle.yml; thumbcache.py:265, viewer.py:109 use QImageReader) — a decoder security update requires a full app release; no pool interface exists to version
- **Gap:** Design the decoder-bundle versioning into the pool interface from the start (an interface concern of finding 2, cheap now, expensive to retrofit); the actual out-of-band update channel can land later.
- **Notes:** The doc lists this under the CI-gates section without an explicit milestone; the defensible M1 slice is the versioned interface shape, with shipping-updates machinery deferred (spec maps general maintenance machinery to M4).

### ❌ missing (XL) Isolation coverage for the full M1 format matrix: M1 includes 'stills + RAW rendering, video indexing/playback' (product-spec.md:838-841); the threat model commits RAW (LibRaw-class) inside the pool (doc:93) and 'Video stays ffmpeg-in-A for the foreseeable future' (doc:113)

- **Evidence:** The tracer decodes only what QImageReader handles in-process (thumbcache.py:286, viewer.py:111); no RAW pipeline, no video pipeline, no ffmpeg integration exists anywhere in apps/desktop-python/
- **Gap:** The pool's IPC design (finding 2) must accommodate more than one-shot stills: LibRaw-class RAW decode (large buffers, long jobs) and ffmpeg-class video for indexing (frame grab) AND playback — a sandboxed streaming decode service feeding the UI is a materially harder interface than fd-in/pixels-out, and no design exists for it.
- **Notes:** RAW/video feature work belongs to the formats area; flagged here because designing the M1 pool interface for stills only, then retrofitting streaming video, would force a redesign. The isolation interface should be scoped against the full M1 decoder matrix before implementation.

### ❌ missing (XL) wasm-compiled decoders (mechanism B) 'adopted opportunistically — starting with stills codecs where the wasm port is mature — inside the same worker-pool interface' (docs/decode-threat-model.md:109-113; spec §10 item 12 names wasm's decode-sandbox role, product-spec.md:944-946)

- **Evidence:** No wasm runtime or wasm-compiled codec anywhere in the repo (grep for wasmtime/wasmer matches only docs/decode-threat-model.md:94)
- **Gap:** None required for M1 — 'opportunistically' sets no deadline; mechanism A alone satisfies the M1 isolation requirement. Becomes actionable only after the finding-2 pool interface exists to slot wasm engines into.
- **Notes:** Listed for completeness as the doc's stated trajectory and the seed of the later extension API; it should not be allowed to delay the mechanism-A floor. Not an M1 gate.

---

## catalog, file identity, N6 substrate (read-only slice)

### ✅ done (S) §3 File identity: identity = content hash (sha256) + library-relative path, recorded per photo (N6 substrate)

- **Evidence:** apps/desktop-python/thumbcache.py:277 (sha = hashlib.sha256(data).hexdigest()), thumbcache.py:404 (signals written into catalog); apps/desktop-python/catalog.py:88-92 (Photo.size/mtime/sha256 fields), catalog.py:405-410 (persisted as z/m/x), catalog.py:480 (reloaded); catalog.py:241-260 (rel_paths: library-relative POSIX); test_tracer.py:343-345, 377 (signals filled and survive round trip)
- **Notes:** Substrate is genuinely recorded and persisted. Caveat: identity is recorded but not yet used as a KEY for anything — catalog joins are positional (album members are photo indices, catalog.py:115 and 429-432; fcache binds by ordered rel-path list, thumbcache.py:197-213). Acceptable inside one atomically-swapped disposable cache, but the (hash,path)-keyed state store the spec's duplicate semantics assume is still ahead (M2 tier-2 writes). Byte-identical duplicates each keeping per-location state holds trivially today (one Photo row per path).

### 🟡 partial (M) N6 substrate completeness: identity signals must exist for the library actually being browsed — adopt-mode (--thumbs) catalogs carry no size/mtime/sha256

- **Evidence:** apps/desktop-python/main.py:1021-1030 (adopt path: scan_library + save_catalog with NO build_cache, so signals stay -1/None per catalog.py:88-92); README.md:136-137 ('Adopt-mode catalogs carry no per-file signals (so reconcile sees only adds/removes there)'); confirmed by measurement: cache/tracer-cache/e884a60103aa3275/catalog.json (100k benchmark) has 0 rows with sha256 or size signals
- **Gap:** A background stat+hash backfill pass for adopted caches (the cancel/progress plumbing already exists in build_cache). Without it, the 100k benchmark corpus — the very library the M1 §7 gate runs on — has zero N6 identity substrate and reconcile cannot even see in-place edits there.
- **Notes:** Stat-only backfill (size/mtime) is S; full sha256 backfill of 100k in background needs rate limiting and persistence of partial progress, hence M.

### ❌ missing (L) §3 reconciliation matrix (2×2): same hash + different path → moved/renamed, state follows

- **Evidence:** apps/desktop-python/catalog.py:532-562 (reconcile_walk is a path-keyed add/remove/modify counter — no hash comparison anywhere); test_tracer.py:399-408 (a rename is asserted to count as added=1 + removed=1, i.e. explicitly NOT move-detected); README.md:134-136 ('does not yet do N6 move-detection (the 2×2 hash-path matrix)'); main.py:596-604 (drift response = full rescan + full rebuild, no per-photo matching)
- **Gap:** Implement the matrix in reconcile: bucket disappeared/appeared paths, match by stored sha256 (size as prefilter), carry per-photo state across the match, and the moved+edited fallback (offer match by name/EXIF, surfaced). Also needs the fcache side to re-bind moved entries instead of rebuilding.
- **Notes:** The formal N6 CI gate is M4, so M1 does not owe the matrix itself — and M1's read-only display stays correct after external moves because drift triggers a full rescan and all displayed state re-derives from the .picasa.ini that traveled with the folder. What M1 owes is the substrate (hash + signals recorded: done, except adopt mode — see previous finding). The matrix becomes load-bearing the moment M2 writes app-authored state keyed to photos. Reconcile also runs only at warm start (main.py:536-539); FS events / periodic rescan / folder-open revalidation are M4 scope, out of this audit.

### 🟡 partial (M) §3 matrix: different hash + same path → edited externally: keep state, refresh hash, surface notice

- **Evidence:** catalog.py:553-558 (modified counted via size+mtime); main.py:594-604 (drift → status line 'library changed (+a -r ~m) — reindexing…' then full rebuild, which refreshes every hash wholesale); main.py:587-593 (adopt mode: drift only surfaces a generic snapshot-is-stale message, view left stale)
- **Gap:** Per-photo 'edited externally' notice (the spec's surfaced reconciliation note) does not exist — only one aggregate status-bar line. Keep-state semantics are vacuous while read-only but the notice surface is M1-visible UX (N7 spirit) and should ride the matrix work. Adopt mode never refreshes anything.
- **Notes:** Hash-refresh happens today only as a side effect of rebuilding the whole catalog, not as a targeted per-file re-key.

### 🟡 partial (L) §3 cost model: cheap signals first (size+mtime), full hash only for new/changed/candidate-moved files

- **Evidence:** Cheap half done: catalog.py:532-562 (reconcile_walk stat-only diff, no hashing, cancelable), main.py:571-610 (runs in background thread on warm start). Expensive half violated: main.py:596-604 — ANY drift triggers scan_library + build_cache over the ENTIRE library, re-hashing and re-thumbnailing every photo; README.md:134-136 admits ('rebuilds the whole cache on drift rather than patching incrementally')
- **Gap:** Incremental reconcile: hash only added/changed/candidate-moved rows, reuse unchanged thumbnail blobs (fcache rewrite or append), patch the persisted catalog instead of rebuilding. At 100k photos one added file currently costs a ~12 min full reindex at the measured 139 photos/s. Minor edge: a transient stat OSError writes (-1,-1) into the fresh map (catalog.py:549-551) which compares unequal to stored signals → file counted modified → full rebuild triggered by a blip.
- **Notes:** The rebuild is at least background + atomic swap (main.py:649-663, grid invalidation tested at test_tracer.py:419-442), so §7's 'UI responsive throughout' half is honored; the '≥ initial-index rate / never an event users plan around' half is not, at scale.

### ✅ done (S) N6/§6 footgun 2: paths stored library-relative; nothing keyed to drive letters or mount points

- **Evidence:** catalog.py:74-76 (Photo.rel/folder are library-relative POSIX), catalog.py:241-260 (rel_paths with parity guard incl. drive-root/UNC cases), test_tracer.py:285-302 (Windows drive-root and UNC tests); absolute root appears only inside disposable machine-local artifacts (catalog.py:418 catalog.json 'library'; thumbcache.py:424 fcache sidecar) and is never a key — load_catalog takes root as a parameter and ignores the stored string, bind() compares rel lists (thumbcache.py:197-213); the stored value powers only a mismatch warning (main.py:1034-1040)
- **Notes:** Compliant for the M1 slice: user-state keys are all library-relative; absolute paths live only in tier-3 disposable caches, which the spec permits.

### ❌ missing (M) §3 'Multi-volume state binds to volume UUIDs so drives can remount anywhere' (footgun 2, second half)

- **Evidence:** No volume/UUID concept anywhere in apps/desktop-python (grep for uuid|volume across the app returns only comments about dying volumes); single Catalog.root at catalog.py:119-121
- **Gap:** Volume-UUID binding for roots on removable/network volumes. M4 scope per §9 (N6 gate) — noted only, not an M1 debt. M1's obligation (don't key state to absolute paths) is already satisfied.
- **Notes:** Out of M1 scope; recorded here so the M4 planning sees it. Needs per-platform volume-ID probing (Linux by-uuid, Windows GetVolumeInformation, macOS later).

### ✅ done (S) N3 substrate: catalog + thumbnail cache are disposable machine-local caches; corruption/staleness can never prevent startup; rebuild from nothing works

- **Evidence:** thumbcache.py:5-9 and 216-221 (machine-local cache/tracer-cache/<digest>/, never inside the library); catalog.py:440-512 (load_catalog returns None on missing/corrupt/foreign/old-version → cold walk), catalog.py:376-388 (CATALOG_VERSION gate with documented bump discipline); main.py:1011-1032 (warm-load try, CacheError/OSError → rescan; --rebuild flag at main.py:949-950 forces cold rebuild); atomic write-temp-rename for both artifacts (catalog.py:434-437, thumbcache.py:347-358); tests: test_tracer.py:388-396 (foreign/corrupt catalog rejected), 361-386 (loaded catalog ≡ walked+indexed one)
- **Gap:** The formal N3 rebuild gate (delete every cache → rescan → machine-diff of full user-visible state) is an M2 CI deliverable and does not exist yet; the roundtrip test is the closest proxy. No M1 debt.
- **Notes:** For a read-only app 'zero database-only user state' holds vacuously — everything displayed derives from ini + files. The disposability mechanics (version gate, degrade-to-cold-walk, atomic swap) are real and tested.

### 🟡 partial (M) §7 catalog-size budget row (M1 read-only gate per §9): ~50 bytes/photo core catalog

- **Evidence:** Measured this audit: cache/tracer-cache/e884a60103aa3275/catalog.json (100k benchmark, adopt mode) = 5,229,874 B / 100,000 photos = 52.3 B/photo — but with ZERO identity signals in any row; the fully-indexed fixture catalog (cache/tracer-cache/6ca5b6e6f526adb5/catalog.json) = 154.2 B/photo, sample row mean 147 B, where the hex sha256 alone is ~71 B/row. Code admits it: catalog.py:368-372 ('lands well above the spec's ~50 B/photo binary-catalog target'), README.md:133-134. No CI measurement: .github/workflows/tracer.yml is a smoke run only (line 63, --quit-after-ready, no size assertion)
- **Gap:** The honest fully-indexed number is ~150 B/photo, ~3× budget, dominated by the hex sha256. Fix options: binary or base85/truncated hash encoding (~110 B saved gets near budget even in JSON), or the compact binary catalog the code already names as future work — plus a CI row that measures bytes/photo on the 100k corpus so the M1 gate is machine-checked. Alternatively argue the budget (spec says ~50 B is oracle-measured Picasa; Picasa stored no content hash).
- **Notes:** Do not let the 52.3 B/photo adopt-mode number be quoted as green — it is the budget met by omitting the N6 substrate. The two requirements (50 B/photo AND per-row sha256) are in direct tension in JSON; this likely warrants a spec argument (§10 style) or the binary-catalog work.

### ❌ missing (XL) §3 'A library = one library-home directory plus N watched roots, possibly on other volumes' — multi-root libraries

- **Evidence:** Single positional root only: main.py:937-941 (one 'library' arg), main.py:986 (_resolve_library returns one root); catalog.py:119-121 (Catalog has exactly one root); thumbcache.py:216-221 (cache dir keyed by the single root path); catalog.py:1-11 and 164-174 (the load-bearing walk-order parity ties catalog↔fcache to ONE sorted walk of ONE root); no library-home directory concept exists anywhere
- **Gap:** N-root support touches every identity key: per-photo keys become (root-id, rel) not just rel; the catalog↔fcache positional parity must be defined across roots; cache_dir_for must key on the library, not a root path; and the library-home directory (tier-2 anchor) has no code home yet. Needs design before implementation — the walk-parity invariant is the hard constraint.
- **Notes:** What M1 owes vs M2/M4: watch policy and live watching are M4; tier-2 library-home state files are M2+. But M1 is 'read-only browser over an existing Picasa library', and real Picasa libraries ARE N watched roots across volumes — the owner's family archive likely cannot be browsed as one root without a lowest-common-ancestor hack. Minimum M1 debt: either (a) multi-root ingest read-only, or (b) an explicit, argued deferral plus not deepening single-root assumptions (the digest-keyed cache dir and single-root walk parity are exactly such deepening). Adjacent good news: open-by-path with no required global pointer (footgun 1) is satisfied — config.json is a per-user last-opened convenience only, overridable by path (main.py:124-155, 170-187).

---

## §7 performance gates + CI coverage (M1 read-only rows, N4-on-CI gate, survey cross-check)

### 🟡 partial (M) §7 row: cold start, already-indexed library → interactive grid < 2 s (M1 read-only row; N4 test says it becomes a CI gate at M1)

- **Evidence:** Instrumentation: apps/desktop-python/main.py:1058-1098 (READY line + {event:'ready', cold_start_ms, prep_ms, warm, vm_rss_mb} JSON). Measurements (one-off, dev machines): apps/desktop-python/README.md:31-34 (Linux 467 ms warm-load / 1893 ms cold-walk at 100k) and README.md:47-50 (Windows dev box 511 ms warm / 2.1 s cold-walk). CI runs the app (.github/workflows/tracer.yml:58-63) but on a 10-photo smoke library with no assertion on cold_start_ms.
- **Gap:** No automated gate: no CI step parses the ready JSON or asserts < 2000 ms, and the 100k corpus never reaches CI (/cache/ is gitignored, A:\dev\fauxcasa\.gitignore:12). Measurements are one-off on hardware stronger than the §7 reference (scroll-vsync-validation.md:27-31 'necessary, not sufficient'). Need: a workflow step that runs main.py --quit-after-ready against a real-scale corpus (or cached fcache+catalog) and asserts the budget.
- **Notes:** Note the cold-WALK path (2.1 s Windows) already brushes the 2 s line, but the budget row is explicitly 'already-indexed', which passes with ~4x margin. RSS is also captured at READY (main.py:345-404, cross-platform per fauxcasa-61e) — §7 has no numeric RAM row yet (deferred to stack decision), so nothing to gate there.

### 🟡 partial (L) §7 row: scroll full library — p99 frame time ≤ 32 ms, no frame > 100 ms, scripted flick ≤ 3 screens/s, zero blank tiles, grid never reads originals

- **Evidence:** Harness exists and emits machine-checkable verdicts: apps/desktop-python/bench_scroll.py:577-588 (pass_p99_le_32ms / pass_max_le_100ms / pass_zero_blank / occlusion_clean). Measured: docs/research/scroll-vsync-validation.md:118-146 — 9 clean runs at 100k, p99 5.7 ms worst-max 10.1 ms, zero blanks, incl. the 3.0 screens/s budget edge; raw runs in docs/research/ncv-results/scroll-final.jsonl (all 9 runs platform=wayland). Never-reads-originals is by construction (grid.py decodes only fcache; README.md:23).
- **Gap:** Linux-only, one-off, dev machine that EXCEEDS reference hardware (16-thread/780M vs 8-thread reference — scroll-vsync-validation.md:27-31, 195-206). Zero Windows scroll runs exist despite spec §8 committing §7 budgets to Windows CI (formally M4, but M1 needs the gate green somewhere reproducible). Not runnable in hosted CI as-is: the methodology requires a real display/compositor (bench_scroll.py:369-379; the whole doc exists because offscreen intervals lie). A CI mode would gate on frame-production time (paint_cost — throttle-independent, measurable offscreen) but that offscreen-gate mode and its runner-variance policy don't exist.
- **Notes:** '✓ measured (Linux)' in the task framing is accurate. The reference-class-hardware run is the explicitly-owed sufficiency test (stack-balloons.md tripwire, tracked residual on fauxcasa-hw0).

### ❌ missing (S) §7 row: search keystroke → filtered grid < 50 ms

- **Evidence:** Feature exists but has zero timing instrumentation and zero recorded measurement: apps/desktop-python/main.py:873-889 (_search_changed — a per-keystroke linear Python scan over all catalog photos, no perf_counter anywhere near it); no search metric in the READY/indexed JSON, none in bench_scroll.py, none in apps/desktop-python/README.md or docs/research/scroll-vsync-validation.md. README.md:139-140 admits 'Search is a linear scan (fast enough ≤100k)' — an unmeasured claim.
- **Gap:** Nothing measured, nothing gated. (1) Instrument: time _search_changed and emit a search_ms event / add a scripted probe (e.g. --search TERM after READY) — hours. (2) Measure at 100k: a CPython linear scan doing lowercase+substring over 100k photos per keystroke plausibly violates 50 ms; unverified either way. (3) If it fails, the per-photo word index the spec names becomes real M1 work (L). Gate wiring rides the same CI-perf-gate design as the other rows.
- **Notes:** S is for instrument+measure only; budget-miss remediation (word index) would be L. This is the only §7 M1 row with no data at all.

### 🟡 partial (M) §7 row: initial index (no faces) incl. content hashing — library browsable immediately; ≥ 30 photos/s local volume, ≥ 10/s slow volume

- **Evidence:** Hashing is genuinely in the measured path: apps/desktop-python/thumbcache.py:250,277 (sha256 per photo during index). Rate instrumentation: main.py:630-634 ({event:'indexed', rate_per_s, workers}). Measured: apps/desktop-python/README.md:35-42 — 139 photos/s in-app on realistic 3.3 MB/8 MP photos, 8 workers, Linux dev box (~4.6x the 30/s budget). Browsable-while-indexing is wired (tiles appear live during build; README.md:64-65).
- **Gap:** (1) Reference-hardware run still owed — named the residual risk on fauxcasa-hw0 (scroll-vsync-validation.md:203-206; spec §9 M0 note). (2) Slow-volume ≥ 10/s: never measured at all. (3) Full-fidelity gate needs the size-scale 1.0 corpus (≈500 GB) — scripts/make-synthetic-library.py:27-31 says the default 0.02 scale (22 GB) is NOT valid for index-rate gates; nobody has run the full-size distribution. (4) No CI assertion of rate_per_s (tracer.yml checks exit codes only).
- **Notes:** The offline v2 cache-build rate (≈3x per-photo work) was explicitly not benchmarked (README.md:54-58) — a secondary honesty gap, since the '≥ initial-index rate' full-rebuild row inherits it.

### ❌ missing (L) §7 row: catalog size ~50 bytes/photo core catalog; cache total ≤ 2% of library

- **Evidence:** Deliberate deviation, documented in code: apps/desktop-python/catalog.py:361-372 — persistent catalog is JSON and 'lands well above the spec's ~50 B/photo binary-catalog target — a compact binary catalog is future work' (a hex sha256 alone is 64 B/photo). README.md:133-135 says the same. Cache side: v1 fcache 377 MB / v2 1.5 GB at 100k (README.md:43-47); no measurement or check exists against the ≤ 2% row.
- **Gap:** No gate, no measurement step, and the row as written can't be evaluated honestly on the size-scaled benchmark: v2's 1.5 GB is ~7% of the 22 GB (0.02-scale) library but ~0.3% of the 500 GB reference composition — a gate must normalize to reference composition or run on the full-size corpus. Work: (a) cheap — add a catalog+cache size report to the ready/indexed JSON and a normalized check (S); (b) real — the compact binary catalog to actually hit ~50 B/photo (L), or re-argue the row's M1 applicability in the spec (it may be a product-store requirement, not a tracer one).
- **Notes:** The tracer README is honest here; the audit point is that M1's N4 slice names 'catalog size' as a gated read-only row and today it is neither met nor tracked by any automation.

### 🟡 partial (L) §7 row: external change → visible in UI < 5 s (local FS events) — gates at M4, noted for M1 substrate only

- **Evidence:** Out of M1 scope (spec §9/§2: watch rows gate at M2/M4). Read-only substrate that exists: startup background reconcile diffs cheap size/mtime signals and atomically swaps the rebuilt catalog (apps/desktop-python/main.py:~580-610 _start_reconcile / reload_data at 649-663; README.md:22).
- **Gap:** For M4, not M1: no FS-event watcher, no runtime polling — external changes are only picked up at next launch. No M1 action required; flagging so the M4 owner knows the substrate is startup-only reconcile with whole-cache rebuild (no incremental patch, no N6 move-matrix).
- **Notes:** Size is the eventual M4 effort, not an M1 ask.

### ❌ missing (XL) M1 gate: 'N4 budgets green ON CI' on the 100k synthetic library (§2 N4: each budget row becomes an executable CI gate at its milestone; §9 M1 gate line)

- **Evidence:** Full CI inventory — four workflows, none asserts any §7 number: .github/workflows/tests.yml:22 (parser suite, ubuntu+windows); tracer.yml:39-63 (tracer unit tests + headless e2e smoke + warm-cache second run, ubuntu+windows, offscreen, 10-photo generated library, exit-code-only); bundle.yml:47-229 (frozen PyInstaller artifact: build, smoke run, non-blank-screenshot assert, graceful no-arg/no-display exits, windowed-build log assert, soft 200 MB size warn — all functional); balloons.yml (build-only). No workflow parses the READY/indexed JSON, runs bench_scroll.py, or touches a 100k corpus (cache/benchmark-library + fcache are gitignored, .gitignore:12).
- **Gap:** The entire CI perf gate needs design then build: (1) Corpus strategy — grid/scroll/cold gates need only the fcache+catalog, NOT the 22 GB library (scripts/make-synthetic-library.py:28-31 'size-scale-independent'), so options are: cache a built 100k fcache as a CI artifact (377 MB v1 / 1.5 GB v2 — fits GH cache), regenerate a smaller-N golden proxy (e.g. 10k) with scaled thresholds, or both; the index-rate gate is the hard one (needs real file sizes; full fidelity = 500 GB, impossible hosted — needs a smaller-N full-size-distribution proxy or a self-hosted runner). (2) Runner policy — hosted runners are not §7 reference hardware and are noisy; either a self-hosted reference-class runner (also satisfies the 'measured on reference hardware' clause of N4) or explicitly-margined proxy thresholds. (3) Scroll on headless CI — needs the offscreen frame-production-time mode argued above, or lives only on the reference runner. (4) The assertion layer itself is easy: main.py and bench_scroll.py already emit machine-parseable JSON with pass_* booleans.
- **Notes:** This is the single biggest M1 gate hole in this area: every green checkmark in the §7 story today is a one-off dev-machine measurement written into READMEs/reports, not an automated regression gate. What CI does prove today (well): cross-platform functional health, frozen-artifact viability, and graceful-failure UX.

### ❌ missing (M) M1 gate: 'picasa_db.py survey cross-check shows zero ingest loss on synthetic corpora'

- **Evidence:** Both halves exist separately, the cross-check does not: survey side — scripts/picasa_db.py:1287-1357 (_survey_ini_tree counts-only rollup: files, sections, star/caption/album/keyword key counts) with unit tests (scripts/test_picasa_db.py:782-911, run in CI via tests.yml); ingest side — apps/desktop-python/catalog.py scan_library ingests the same fields. Nothing compares them: no test in test_tracer.py references picasa_db survey (grep of apps/desktop-python for 'survey' hits only unrelated README/docstring lines), no script, no CI job.
- **Gap:** Build the cross-check harness: generate a synthetic corpus with known ini metadata (make-synthetic-library.py already writes some; may need richer ini coverage — stars, captions, keywords, albums, hidden, folder categories), run `picasa_db.py survey --library` rollup, run scan_library, assert count-for-count equality (zero loss), wire into tests.yml/tracer.yml. Note scope honesty: the M1 spec text says full ingest incl. .pal, contacts.xml, db3 rescue — the tracer ingests ini + in-file JPEG XMP/IPTC only (catalog.py:25-27), so the cross-check can only cover what's ingested; the harness should be built to grow with the ingest surface.
- **Notes:** This gate is CI-friendly today (pure-Python, synthetic, small) — unlike the perf gates it has no corpus/hardware blocker, making it the cheapest missing M1 gate to land.

### 🟡 partial (M) Gate substrate: machine-readable §7 instrumentation (READY protocol + bench harness) suitable for CI assertions

- **Evidence:** Done for cold start / RSS / index rate: main.py:82-92 (the §7 machine protocol deliberately kept on raw stdout, survives console=False builds), main.py:1058-1098 (ready event), 630-634 (indexed event with rate_per_s), 1145-1147 (exit event with RSS), 345-404 (cross-platform RSS probe, fauxcasa-61e). Done for scroll: bench_scroll.py — occlusion-guarded protocol (lines 86-100, 427-438, 577-588) with explicit pass/fail §7 verdict booleans and full provenance; helper unit-tested (test_tracer.py:2345 test_pcts_nearest_rank). Timeouts make scripted runs fail loudly (main.py:1133-1141).
- **Gap:** Two substrate holes: (1) no search-latency probe at all (see the search row); (2) bench_scroll has no headless/offscreen gate mode — its Wayland-native assumptions and real-display requirement (lines 369-379) block hosted-CI use, and occlusion_clean semantics would need rethinking offscreen. Otherwise the substrate is genuinely gate-ready; what's absent is any consumer that asserts on it.
- **Notes:** The instrumentation quality here is high (adversarially pre-mortemed methodology, occlusion tells, provenance fields) — the deficit is purely that nothing automated reads the numbers.

### ✅ done (S) CI coverage today: green functional builds/tests on Linux AND Windows for the tracer and parsers (the 'green builds from day one' evidence rule the M1 gates will ride on)

- **Evidence:** .github/workflows/tests.yml:13-22 (parser suite, ubuntu-latest + windows-latest, every push/PR; committed oracle fixtures run, private-cache tests self-skip); tracer.yml:22-67 (unit tests + generated smoke library + end-to-end headless run with screenshot + warm-cache second run, both OSes); bundle.yml:24-247 (pinned PyInstaller frozen artifact on both OSes: smoke run, decoded-thumbnail assert, graceful-exit asserts, windowed-log assert, soft size warn, artifacts uploaded); balloons.yml (4 stacks build, both OSes).
- **Notes:** Solid functional CI foundation; every workflow is exit-code/behavioral, zero performance assertions. macOS absent everywhere (consistent with spec §8 hardware gating).

---

## §4 merge precedence + import diagnostics (Fauxcasa tracer, M1)

### 🟡 partial (S) §4: in-file metadata wins for tier-1 data — captions and keywords

- **Evidence:** catalog.py:13-23 (documented precedence design), catalog.py:301-309 (ini caption/keywords filled at scan), thumbcache.py:405-413 (indexer overrides with in-file XMP/IPTC only when truthy), inmeta.py:29-31,259-260 (XMP wins over IPTC per field within the file), test_tracer.py:728-758 (override + no-in-file-keeps-ini), test_tracer.py:1406-1424 (empty in-file caption does not clobber ini), test_tracer.py:761-772 (merged result survives catalog persist/load)
- **Gap:** Two holes: (1) adopt mode (--thumbs, the path every 100k-scale library uses today) binds an external cache without running the indexer, so its catalog stays ini-only forever — precedence is simply never applied (catalog.py:20-23, README.md:152-157 admit this). (2) The in-file read is JPEG-only (inmeta.py) — defensible because Picasa uses ini caption=/keywords= for non-JPEG, but TIFF XMP written by other tools is invisible. Fix for (1): a metadata-only pass (read bytes, skip thumbnailing) when adopting a cache, or an explicit backfill step.
- **Notes:** The core rule is real, correct, and well-tested for the indexed path; this is the healthiest requirement in the area. The 'empty in-file value loses to ini' tiebreak was decided by reasoning, not oracle observation — fold into the hardening item below.

### ❌ missing (L) §4: in-file metadata wins for tier-1 — faces-in-XMP

- **Evidence:** inmeta.py:14-19 ('Two fields only — caption and keywords... Faces-in-XMP, geotags, dates... are out of tracer scope'), catalog.py:27 ('faces-in-XMP, geotags, and in-file dates are not ingested'), no faces field on Photo (catalog.py:74-93). Ini-side ingredients exist unused: scripts/picasa_db.py:719-742 (parse_faces for ini faces= lines, incl. UNKNOWN_CONTACT semantics) is never called by catalog.py
- **Gap:** No face ingestion from ANY store (XMP regions, ini faces=, contacts.xml, db3), so the faces half of tier-1 precedence has nothing to operate on. M1 (§9) requires faces *displayed*. Needs: XMP face-region reading (spec §5 P1 says wrap a mature metadata library, not extend the hand-rolled inmeta walker), ini faces= wiring (parser exists), a contacts.xml parser (does not exist anywhere in the repo), and the merge rule in-file-wins across them.
- **Notes:** Overlaps the full-ingest/faces-display audit area; counted here because the precedence rule is unimplementable until at least two face sources are read. contacts.xml parser absence is the design-free part; XMP face regions via exiv2/exiftool-class wrapper may push toward XL if the 'wrap, don't hand-roll' decision is opened now.

### 🟡 partial (S) §4: ini wins for app state (stars, albums, edit recipes)

- **Evidence:** Stars: catalog.py:302 (ini star= flag, only source). Albums: catalog.py:317-321,335-347 (ini albums= tokens + [.album:uid] sections, only source). Edit recipes: only rotate= is ingested (catalog.py:310-315); filters= parsing exists unused (scripts/picasa_db.py:772). No db3/.pal/contacts read anywhere in the app (grep for db3/pmp/pal/contacts in apps/desktop-python hits only a comment, catalog.py:228)
- **Gap:** The rule holds vacuously: ini is the ONLY store read, so there is no second source it could win against. It becomes a real, testable precedence rule only once db3 (and .pal) are ingested — at that point the work is small (never let db3 override an ini-provided value) but it must be written and tested deliberately. Edit recipes beyond rotate= are not ingested at all (arguably display-only M1 needs just rotate; the edit-stack display is M3).
- **Notes:** Size is for the precedence rule itself, conditional on the db3-rescue ingest landing (sized separately below). Star is stored as bool, consistent with 'legacy star=yes imports as 1 star' for read-only display.

### ❌ missing (L) §4: db3 fills gaps only (the rescue classes)

- **Evidence:** catalog.py imports picasa_db (line 45) but calls only read_picasa_ini and parse_rotate; validated db3 readers (scripts/picasa_db.py:192 read_pmp, 313 read_table, 440 read_thumbindex) are unused by the app. §9 M1 explicitly lists 'db3 rescue' in the full-ingest scope under the §4 precedence
- **Gap:** Wire the db3 catalog into ingest: locate the db3 dir, join thumbindex paths to catalog photos (path translation from absolute Windows paths to library-relative), then gap-fill only — album order, contacts-only face names, video overrides, manual sort (the four DB-only rescue classes) — never overriding an ini or in-file value, with every disagreement recorded as a conflict. Parsers are done and oracle-validated; the join + gap-fill + conflict-detection logic is the new work.
- **Notes:** Path translation (drive letters → library-relative, §8) and choosing which rescue classes M1's read-only display actually needs are the design-ish edges, but the format layer is fully de-risked, so L not XL. Overlaps the full-ingest audit area.

### ❌ missing (M) §4: every conflict is surfaced in the import report, never silently resolved

- **Evidence:** No import report artifact, data structure, or log anywhere: grep for report/diagnos/conflict in apps/desktop-python finds only the rotating diagnostics log (applog.py:6-10, main.py:89) and perf reporting. Three silent resolutions exist today: (1) in-file overrides ini caption/keywords with zero recording even when both are present and differ (thumbcache.py:405-413); (2) diverging duplicate album definitions resolve first-wins in walk order, self-described 'a known-arbitrary choice' (catalog.py:333-334); (3) orphaned album tokens 'skip silently' (catalog.py:330-331)
- **Gap:** THE import-report deliverable: a conflict/anomaly collector threaded through scan_library + build_cache (record source, field, winning value, losing value, photo rel), persisted as a machine-readable report beside catalog.json, summarized in the applog and in a minimal UI count (e.g. status bar 'N import notes'). Substrate only — the interactive 'what does the app think about this file?' inspector is N7/M2 and stacks on top. Sizing the substrate: M (~a day) as long as the merge inputs stay ini+in-file; each new store (db3, contacts.xml) adds its conflict classes to an existing collector at small marginal cost, which is exactly why the substrate should land before the db3 ingest, not after.
- **Notes:** This is the requirement the prompt asked to size. Recommended shape: an ImportReport dataclass owned by Catalog, JSON-persisted (matches the existing catalog.json posture), version-bumped via CATALOG_VERSION. Building it now converts findings (2) and (3) above from silent to surfaced nearly for free.

### ❌ missing (S) §3: copied-in folder whose ini references unknown album UIDs gets placeholder albums, surfaced in import diagnostics, never dropped

- **Evidence:** catalog.py:330-331 ('orphaned tokens are documented ini/db drift — skip silently'), catalog.py:354-356 (membership resolution: `if uid in albums` — unknown UIDs dropped from the catalog entirely); sidebar shows only resolved albums (main.py:825-834)
- **Gap:** Create a placeholder Album (uid, synthesized name e.g. 'Unknown album 4dfe636c', a placeholder flag) for every albums= token with no [.album:uid] definition, populate its members, mark it visually in the sidebar, and emit an import-report entry. Directly contradicted by current code, but mechanically trivial once the report substrate exists; the Album dataclass and sidebar already handle everything else.
- **Notes:** Depends on the import-report substrate for the 'surfaced in import diagnostics' half; the never-dropped half (placeholder album in catalog + sidebar) is independent and could land first.

### 🟡 partial (M) §4: provisional precedence 'to be hardened empirically against the oracle before M1 exit' (§9: 'under the §4 precedence (pinned by M1 exit)')

- **Evidence:** Parser-level golden fixtures run in CI on every push (tests.yml:1-4,22; scripts/test_picasa_db.py:1130+ replays fixtures/oracle/NNN-*), and inmeta's model of where Picasa stores captions/keywords cites fixtures 002/003 (inmeta.py:6-11). But the app-level precedence tests are synthetic-only (test_tracer.py:81,1671 — hand-built XMP/IPTC/ini, no fixtures/oracle usage anywhere in test_tracer.py), and no oracle fixture exercises a cross-store DISAGREEMENT (ini vs in-file vs db3 divergence); the 33 fixtures (001-033) each capture one UI action, not conflict states
- **Gap:** Two pieces to 'pin by M1 exit': (1) replay relevant oracle fixture after/ trees through scan_library+build_cache and assert the merged catalog matches Picasa's own view (captions from 002, albums from 003/011/025, stars from 001/010/033, hidden from 017/024/032 — the last is already done at parser+catalog level); (2) author conflict fixtures in the Wine oracle (e.g. edit ini caption after in-file write, foreign XMP change) to observe which value Picasa itself displays, and encode that as the empirical precedence tests — including the currently reasoning-only 'empty in-file loses to ini' tiebreak. Then declare the precedence pinned in the spec.
- **Notes:** Oracle sessions require the Wine machine (not public CI); the committed-fixture-replay half is pure test work and CI-safe per the §4 golden-fixture posture.

### ❌ missing (M) §9 M1 gate: 'picasa_db.py survey cross-check shows zero ingest loss on synthetic corpora' (the ingest-loss diagnostic)

- **Evidence:** The survey CLI exists and is well-tested standalone (scripts/picasa_db.py:35, scripts/test_picasa_db.py:782-951,1111), but nothing compares its rollups (photo counts, starred/captioned/album feature counts) against the app's Catalog; no workflow or test references such a cross-check (grep across .github/workflows and test_tracer.py)
- **Gap:** A harness that runs scan_library+build_cache and `picasa_db.py survey --library --json` over the same synthetic corpus and diffs the counts (photos, starred, captioned, keyworded, album membership, hidden) — the machine-checkable 'zero ingest loss' gate M1 exit names, and the natural consumer of the import report's anomaly counts. Needs a synthetic corpus WITH a db3 dir (make-synthetic-library.py output plus a generated/fixture db3) or a survey mode that runs ini-tree-only.
- **Notes:** Included here because it is the quantitative half of import diagnostics (report = qualitative conflicts, cross-check = aggregate loss). Minor adjacent note, different area: README.md:144-146 still claims the folder-level Hidden Folders category is ignored, but catalog.py:217-238 now honors it per fixture 032 — the honesty ledger is stale on that line.

---

## Draft PR #26 (fauxcasa-q7m, feat/q7m-grid-hidpi) — grid hi-DPI fcache-v2 consumer: code completeness per the bead, merge state vs main, and viability of the blocking dpr>1 scroll-fps/RSS measurement on this Windows dev box

### ✅ done (S) Bead core: grid decodes the nearest fcache v2 level >= tile*devicePixelRatio via best_level()/entry(), instead of always reading the 256 primary and scaling-in-paint

- **Evidence:** Branch grid.py:370-371 (`native = self._tile_native; offset, length, _w, _h = thumbs.entry(idx, thumbs.best_level(native))`); grid.py:537 (`want = max(TILE_NATIVE, round(TILE_NATIVE * self.devicePixelRatioF()))`); API verified on main at apps/desktop-python/thumbcache.py:109-118 (best_level, smallest-sufficient over descending levels) and :120-124 (entry). Decoded tile capped to `native` (branch grid.py ~386-392); paint blits with SmoothPixmapTransform (branch grid.py:563,591).
- **Gap:** None in the code itself. Note one deliberate deviation from the bead's literal wording: 'tile*DPR' is implemented as max-tile(TILE_NATIVE=256)*DPR, not current self.tile*DPR, to preserve the z1e one-decode-per-photo invariant (zoom never re-decodes). Documented in grid.py:78-93 and the PR body; it is the right reading, but the bead leaves 'DPR-aware vs zoom-aware' an explicit measurement call, so the measurement (below) must confirm it.
- **Notes:** Integration test test_grid_decodes_dpr_scaled_v2_level (branch test_tracer.py:2124) proves the worker reads the 512 vs 256 blob by asserting the decoded long edge.

### ✅ done (S) Bead core: no-op at devicePixelRatio == 1 (the §7 numbers, measured at dpr 1, must be unchanged by construction)

- **Evidence:** best_level(256) returns the 256 level for both v1 [256] and RECOMMENDED_LEVELS (512,256,128) — thumbcache.py:43,109-118; floor `max(TILE_NATIVE, ...)` at branch grid.py:537; unit test test_refresh_tile_native_dpr_and_invalidation (branch test_tracer.py:2216) covers dpr 1.0 -> 256, sub-1 floor, fractional 1.5 -> 384, and invalidate-only-on-change. CI green on the branch (all 12 checks SUCCESS per `gh pr view 26`).
- **Gap:** None. The documented edge case (a hand-built level set lacking 256, e.g. [512,128], reads 512-capped-to-256 instead of the old primary) is real but unreachable by any shipped config — grid.py:86-90 comment says so, and build_cache defaults to [256].
- **Notes:** DPR read on the GUI thread in paintEvent (branch grid.py:548 -> _refresh_tile_native at :528-540) because devicePixelRatioF needs a live screen; workers read a plain int — GIL-safe; generation bump via _invalidate_tiles (branch grid.py:267) discards stale in-flight decodes on a monitor-move ratio change.

### ✅ done (S) Branch merges cleanly with current main (a02ba09) — specifically vs the sidebar-rebuild fix and the fauxcasa-uix/61e portability commits that landed after the PR was cut

- **Evidence:** `git merge-tree --write-tree origin/main origin/feat/q7m-grid-hidpi` exits 0, no conflicts (tree 727f9a8); GitHub reports mergeable=MERGEABLE, mergeStateStatus=CLEAN. a02ba09 touched only main.py + test_tracer.py (PR does not touch main.py; its test_tracer.py changes append at EOF, main's were elsewhere). Verified the merged decode-worker region is semantically coherent: PR's `entry(idx, best_level(native))` combines with main's O_BINARY open + lseek/read (merged tree, grid.py ~lines 380-416).
- **Gap:** No textual or semantic merge conflict — but the branch is 14 commits behind (merge-base 0adb4d9) and is functionally broken on Windows AS-IS: branch grid.py:373 still uses os.pread (every thumb becomes an error tile on Windows; main fixed this in bcb5faf), and branch bench_scroll.py lacks the Windows GetProcessMemoryInfo RSS path (grep count 0; main added it in 06a72a2/be663ff). Rebase or merge main into the branch BEFORE any local measurement.
- **Notes:** Rebase is mechanical (verified clean).

### 🟡 partial (S) PR's new integration tests remain correct after rebase: skipif(not hasattr(os,'pread')) with reason 'grid decode worker uses os.pread (Unix-only)'

- **Evidence:** Branch test_tracer.py:2122,2175 carry the skipif; main's bcb5faf (grid.py:355-361 on main) removed os.pread from the worker, so post-merge the skip reason is factually false and the two tests (test_grid_decodes_dpr_scaled_v2_level, test_grid_v1_cache_falls_back_to_only_level) would silently skip on Windows even though the code path they test now runs there.
- **Gap:** After rebasing, drop the two skipif markers (and the PR-body claim 'the worker's blob read is Unix-only', now stale) and run the full suite on this Windows box — the two formerly-skipped tests should pass here, upgrading the PR's Windows validation from 'skips' to 'passes'.
- **Notes:** This is the only code change the rebase actually requires; everything else merges as-is.

### ❌ missing (S) Bead merge-blocker: 'Needs its own bench_scroll.py measurement (scroll-fps + RSS at dpr>1) ... before it can land'; spec §7 gate = p99 frame ≤ 32 ms, no frame > 100 ms at ≤ 3 screens/s, zero blank tiles (docs/product-spec.md:761), RSS vs the ~600 MB working target (grid.py CACHE_BYTES comment, dpr-1 peak was ~590 MB)

- **Evidence:** No dpr>1 bench run exists anywhere: PR is draft for exactly this (PR body 'What still needs measuring'); bead notes say 'REMAINING: scroll-fps + RSS re-measurement at dpr>1'; README.md:50-52 records only that scroll perf is unchanged *until* q7m reads the 512 level.
- **Gap:** Run bench_scroll.py at dpr 2 (and dpr 1 control) on the merged tree and record frame_ms_p99/max, blank_tile_frames, vm_rss_mb/vm_hwm_mb; then make the DPR-aware vs zoom-aware call. The RSS risk is concrete: at dpr 2 the min-zoom (64 px tile) want-band holds 512 px tiles at ~1 MB each and the want-band is never evicted, so the dpr-1 ~590 MB peak likely exceeds the working target — if it does, either lower CACHE_BYTES/zoom-aware sizing (M, fauxcasa-5br territory) or land with a documented headroom bead.
- **Notes:** S for the measurement itself (each run ~3.5 min: 2x45 s flicks + 4 teleport dwells); M contingency only if RSS fails and zoom-aware sizing is chosen.

### ✅ done (S) Can that dpr>1 measurement validly run on THIS Windows dev box (the PR body claims it must go to maphew on Linux because 'neither gate is measurable' on Windows)?

- **Evidence:** Verified empirically on this box: (1) real display DELL S3221QS, 4K@60 Hz, native devicePixelRatioF()=1.25 (Windows 125% scaling) — the hi-DPI path is active even unforced (native=320 -> best_level picks the 512 level); (2) QT_SCALE_FACTOR MULTIPLIES the platform DPR: QT_SCALE_FACTOR=1.6 -> exact dpr 2.0, QT_SCALE_FACTOR=0.8 -> exact dpr 1.0 baseline, both on the native 'windows' plugin (probed via PySide6, output above); (3) bench_scroll.py runs on Windows (native-plugin fallback, bench_scroll.py:369-379, from PR #25 — already in the merge-base) and reports device_pixel_ratio in its JSON (bench_scroll.py:408,526); (4) RSS is measurable on Windows via GetProcessMemoryInfo (bench_scroll.py:244-282, on main); (5) fixtures are present locally: cache/benchmark-library (100k photos), cache/benchmark-thumbs.fcache v2 levels [512,256,128], cache/benchmark-thumbs-v1.fcache [256] for the fallback comparison (verified by loading both).
- **Gap:** The PR body's premise is OUTDATED: both gates became measurable on Windows via main commits (bcb5faf portable read, 06a72a2 xplat RSS) that landed AFTER the PR was cut — but ONLY on the rebased/merged tree, never on the branch as-is (os.pread would error-tile every thumb, invalidating the run). Two validity caveats: (a) Windows numbers are not directly comparable to the canonical Linux §7 baselines (README.md:29 labels them Linux; different compositor/GPU) — a same-box dpr2-vs-dpr1 pair is a valid RELATIVE gate signal and fully answers the DPR-aware-vs-zoom-aware and RSS-headroom questions; the canonical Linux re-baseline stays with fauxcasa-k5p as a follow-up, not a merge blocker; (b) runs need an unoccluded real window for ~3.5 min each (occlusion_clean at bench_scroll.py:586-588; the Wayland ~100 ms frame-callback-timeout tell is inert on Windows). QT_QPA_PLATFORM=offscreen would NOT be a valid run — bench_scroll exists precisely to retire headless numbers (bench_scroll.py:6-13); use the real display with QT_SCALE_FACTOR.
- **Notes:** Concrete land-it plan: (1) rebase feat/q7m-grid-hidpi onto main (clean, verified), drop the two stale pread skipifs, full suite on Windows — the 2 integration tests now pass locally [S]; (2) measurement matrix on this box, v2 cache: QT_SCALE_FACTOR=0.8 (dpr 1.0 control), unset (native 1.25), QT_SCALE_FACTOR=1.6 (dpr 2.0 gate) — each at default zoom and --zoom min (worst-case RSS: never-evicted want-band of ~1 MB tiles); optional dpr-2 run against benchmark-thumbs-v1.fcache to confirm the v1 fallback stays at 256 [S]; (3) gate check: frame_ms_p99<=32 / max<=100 / blank==0 and vm_hwm_mb vs ~600 MB — pass -> record numbers in README §7 + bead, keep DPR-aware sizing; fail on RSS -> zoom-aware sizing or CACHE_BYTES cut before merge [M contingency]; (4) update PR body (Unix-only claim, Linux-handoff premise), un-draft, merge, close fauxcasa-q7m; file/point the Linux canonical dpr>1 re-baseline at fauxcasa-k5p.

---

## Completeness critic

A final agent checked the matrix against the spec text for coverage holes,
over-optimistic statuses, and unowned gate clauses. (Its input matrix was
truncated at ~180 KB, so its 'missing area' meta-note is an artifact; the
matrix above is complete.)

### Requirements no area covered

- §3 'Cache location — settled procedure' (product-spec.md:306-315): 'The default is decided empirically at M1: run the §7 gates on the slow-volume profile with the cache machine-local vs in-library (and the hybrid)'. This is an M1-pinned measurement/decision and NO finding in any area mentions it. It also depends on a slow-volume test profile that no area has built (see gate gaps).
- §5 Maintenance & trust (product-spec.md:573): 'i18n string externalization from day one (full localization is post-v1)'. No finding covers it; verified no tr()/QTranslator/gettext anywhere in apps/desktop-python — every UI string is a hard-coded literal. If the tracer is exempt as disposable, that exemption is recorded nowhere.
- §4 'Read: everything' explicitly lists .picasaoriginals (product-spec.md:364-366). The app only classifies stash folders as hidden (catalog.py:206-210); no finding covers ingesting the stashed-original ↔ edited-photo association (a photo 'has a Picasa-saved original' is displayable read-only state). Caveat: §9 M1's ingest parenthetical (ini, .pal, contacts.xml, db3 rescue, XMP) omits .picasaoriginals, so its milestone is ambiguous — needs an owner pin either way; currently unowned by any area.
- §5 Organize (product-spec.md:477-478): album 'year grouping' as part of M1 'albums displayed'. Only appears as a buried note inside the ini area's albums finding (Album.date ingested but unsurfaced); no finding owns year-grouped album display in the sidebar.
- META: the matrix is truncated. The brief says 16 area agents but only 15 areas are present, and the 15th ('§4 merge precedence + import diagnostics') is cut off mid-finding (last entry ends at 'requ'). At least the tail of that area — and possibly one entire area's findings — is absent and therefore unaudited. Recover the missing output before treating this audit as complete.
- §2 N7 spirit at M1 (soft): the --min/--max-image-size scan filters (main.py:951-958) silently exclude photos with no inspectable UI surface — exactly one of N7's five documented invisible hide mechanisms. N7 formally gates at M2, so this is a note, but no finding covers the size filter's inspectability at all (the ini area covers only hidden=yes/stash-folder reveal).

### Findings judged too optimistic

- ini area, 'caption= read with §4 tier-1 precedence … surfaced' (status: done) and its sibling 'keywords= read … surfaced' (done): too optimistic and inconsistent with the in-file XMP area, which rates the identical substance 'partial'. Adopt mode (--thumbs) — the only path any 100k-scale library including the benchmark corpus uses — skips the indexer, so tier-1 precedence is never applied and the catalog stays ini-only (catalog.py:20-23, README.md:152-157). For an M1 'full ingest under §4 precedence' requirement, 'partial' is the correct status.
- catalog/N6 area, '§7 catalog-size budget row' (status: partial): contradicts its own notes. The honest fully-indexed number is ~150 B/photo (~3× the ~50 B budget) and there is zero CI measurement; the on-budget 52.3 B/photo figure is the adopt-mode catalog that omits every N6 identity signal the same audit calls M1 substrate. The §7 area rates the same row 'missing' — that status is right; 'partial' launders a failed-or-unmeasured gate.
- search area, 'Instant as-you-type search over filenames' / 'captions' / 'keywords' (three findings, status: done): the requirement's operative word is 'Instant', and the same area rates the <50 ms §7 row 'missing — zero data at all' over a per-keystroke linear scan that re-lowercases every name/caption/keyword across the catalog. Functionally present, but 'done' against the spec text is optimistic until measured at 100k; should be partial (functionality present, budget unverified).
- faces+people area, finding 1 'parse ini faces= into the catalog' (status: partial): the app-side ingest is entirely absent — no Photo.faces field, no call site; the partial credit rests on an unused parser in scripts/picasa_db.py. The ini area rates the same requirement 'missing', which is correct since the requirement text is ingest INTO THE CATALOG. Cross-agent inconsistency; resolve to missing.
- ini area, 'Ini freshness: catalog reflects current on-disk ini state on warm start' (status: partial): nothing implements the stated requirement — the warm path never re-reads inis and reconcile_walk never stats them (catalog.py:440-449, 532-562), so an externally-written star/caption is invisible until an unrelated photo-file change or --rebuild. The partial credit comes from photo-file drift detection, which cannot detect sidecar changes. Closer to missing.
- grid area, 'Live counts everywhere' (status: done): 'everywhere' (§5:438) is quietly narrowed to the four views that exist; the M1 surfaces that must also carry live counts — People per-person counts, Recently Updated, placeholder/unknown albums — do not exist yet (each rated missing elsewhere). Done-as-scoped understates that the requirement cannot be done until those surfaces land.
- stills format area, 'Stills scanning + rendering for JPEG, PNG, TIFF, GIF, BMP, WebP' (status: done): decode capability was verified empirically (supportedImageFormats), but the area's own notes admit zero non-JPEG fixtures or regression tests anywhere — 'rendering' for real-world variants (CMYK/progressive JPEG, 16-bit or multi-page TIFF, animated GIF, etc.) is by-construction inference. Borderline: for a gate-bearing matrix, done-without-any-test-coverage on 5 of 6 formats deserves partial or at minimum a hard caveat.

### M1 gate-clause coverage

- Gate clause 3 — 'owner confirms the same on the family archive' (product-spec.md:844): NOT represented by any finding in any area. Nothing covers what this clause needs: an owner-runnable read-only build plus the survey/ingest cross-check runnable against the private archive (on read-only copies per §9:819-822 and the privacy-real-picasa-data rules, outside the repo), and a place to record the confirmation. No bead tracks it. This is a wholly unowned gate precondition.
- Gate clause 1 — 'N4 budgets green on the 100k synthetic library' (product-spec.md:842): represented (the §7 area rates the CI gate missing/XL and all five N4 M1 read-only rows — cold start, scroll, search, initial index, catalog size — have findings), BUT one defining aspect is unrepresented: the §7 reference library is '100k photos / ~500 GB across TWO volumes' with per-volume-class budgets (product-spec.md:747-751, 764), and no finding covers generating a two-volume synthetic corpus or running any budget row on the slow-volume profile (only the index-rate finding notes ≥10/s was never measured). The single-root library model (catalog area, XL) makes the gate as-specified structurally unrunnable, and no finding connects these facts. The uncovered §3 cache-location M1 measurement rides the same missing slow-volume profile.
- Gate clause 2 — 'picasa_db.py survey cross-check shows zero ingest loss on synthetic corpora' (product-spec.md:843): represented — in fact by FOUR areas (ini, db3, faces, §7/CI) each independently rating it missing and each sizing the same harness at M, with no designated owner. The corpus holes that would make the gate vacuous (no faces=/[Contacts2]/contacts.xml/.pal/video/geotag in make-synthetic-library.py) are also covered. The gap is coordination, not absence: split ownership risks double-counted effort or, worse, four partial harnesses; assign a single owner and treat the other three findings as feature-count contributions to it.
