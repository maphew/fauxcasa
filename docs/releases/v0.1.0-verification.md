# v0.1.0-rc2 manual verification (Windows, real display)

Owner pass per fauxcasa-ez2.11, recorded 2026-09-16. Build under test:
`v0.1.0-rc2` (269d61f), the published Windows bundle, run against a
synthetic library on the 4K reference box with a real display (not
offscreen). Nothing private was used.

## Manual display pass

| Item | Result |
| --- | --- |
| first-run picker and remembered library | pass |
| grid at default zoom | pass |
| grid at min and max zoom | pass |
| viewer: still, RAW, video with audio | pass |
| slideshow | pass |
| hover peek (Ctrl+Alt while hovering a thumbnail) | pass |
| inspector (Info button / `I`) | pass |
| tray hold and clear | pass |
| search with negation (`-term`) | pass, see finding 1 |
| star toggle persists across restart | pass |
| File Types dialog restart | pass |
| Show hidden | pass |
| folder sort menu | pass |
| About and Keyboard Shortcuts dialogs | pass |
| window icon and taskbar identity | pass |
| `fauxcasa-console.exe --version` | pass |

All items pass. Two findings, neither a release blocker:

1. **Search box is nearly invisible on the dark toolbar.** The field reads as
   empty toolbar space; there is no hint that it exists or can be typed in.
   Filed as fauxcasa-e2y.
2. **Light mode could not be verified.** There is no mode switcher: the app
   is dark-only by design (theme.py, fauxcasa-ez2.4). Filed as a decision
   bead, fauxcasa-6y0.

## 100k benchmarks

Not yet run. The scroll-vsync methodology needs an idle machine and the box
was shared with other agent sessions on 2026-09-16. Numbers go into the
release-notes provenance section when recorded.
