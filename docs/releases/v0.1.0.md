# Fauxcasa 0.1.0 — See your library again

Fauxcasa 0.1.0 is a read-only Picasa-library browser: point it at a folder
tree that Picasa used to manage and it shows you the same photos, folders,
albums, people, stars, and captions again, without touching a single file in
your library. It does not edit, tag, export, or write anything back to
Picasa — this release is entirely about looking, not changing.

## Download

- Windows: `fauxcasa-0.1.0-windows-x64.zip`
- Linux: `fauxcasa-0.1.0-linux-x64.tar.gz`
- From source: `uv run apps/desktop-python/main.py ~/Pictures` (see
  [README.md](../../README.md))

Get the archives from the
[latest release page](https://github.com/maphew/fauxcasa/releases/latest).

## Verify

Each release archive is published alongside a `SHA256SUMS` file. Check the
download's hash against it:

```
# Windows (PowerShell or cmd)
certutil -hashfile fauxcasa-0.1.0-windows-x64.zip SHA256

# Linux/macOS
sha256sum fauxcasa-0.1.0-linux-x64.tar.gz
```

Compare the printed hash against the matching line in `SHA256SUMS`.

To confirm an archive was actually built by this project's CI (not
substituted or tampered with in transit), use GitHub's build provenance
attestation:

```
gh attestation verify fauxcasa-0.1.0-windows-x64.zip --repo maphew/fauxcasa
```

This proves CI provenance — that the file came from a GitHub Actions run in
this repository — but it does **not** make Windows trust the binary; see
SmartScreen below.

## Windows SmartScreen

The Windows build is unsigned, so SmartScreen will show an
unknown-publisher warning on first run. Right-click the downloaded zip,
open **Properties**, and click **Unblock** before **Extract All** — or, if
SmartScreen still intervenes at launch, click **More info** then
**Run anyway**.

## First run

The app window appears immediately; it does not wait for the library scan
to finish. The initial walk of your photo folder runs in the background
while the window is already up, and thumbnails fill in as they're indexed.

Fauxcasa never writes into your photo library. Its own rebuildable catalog
and thumbnail cache go to your user cache directory:

- Windows: `%LOCALAPPDATA%\Fauxcasa\cache`
- Other platforms: `$XDG_CACHE_HOME/fauxcasa`, or `~/.cache/fauxcasa` when
  `XDG_CACHE_HOME` is unset

## What works

- Folders (tree and flat view), albums (`.picasa.ini` and `.pal`), People
  and Unnamed faces, Starred, Recently Updated
- Ingest from `.picasa.ini`, `contacts.xml`, and db3 rescue sources
- Captions, keywords, stars (`Space` — stored locally in `stars.json`,
  never written back to the library), faces overlay (`F`), crops shown
- Hidden photos and folders (toggle: Show hidden)
- Search: multi-word AND, `-term` negation, over filename, caption,
  keywords, people, and folder names
- Selection tray (hold `Ctrl+H`)
- Stills including RAW (16 vendors) and PSD
- Video poster images and playback with audio (`P`, `Ctrl+Left`/`Ctrl+Right`
  seek)
- Viewer: 1:1 zoom (`1` / `Ctrl+Alt+0`) with pan, hover peek (`Ctrl+Alt`),
  slideshow (toolbar Play, per-folder header play)
- Info panel (`I`)
- Multi-root libraries via `--add-root` (CLI only)
- Offline-volume tolerance
- File Types panel (Tools menu)

## Keyboard reference

Generated from `apps/desktop-python/keymap.py` `DEFAULT_SCHEME`.

### Application

| Action | Keys | Notes |
| --- | --- | --- |
| `app.play` | `F11`, `Ctrl+4` | Slideshow of the current view (q6l.3). Repurposes Picasa's F11 full-screen; Ctrl+4 is Picasa's own slideshow chord (ed5.12). |
| `app.info` | `I` | Toggle the metadata inspector panel (q6l.25) — this app's own pick (Lightroom-style info toggle); the shortcut corpus documents no Picasa equivalent. Dispatched PER-SURFACE (grid.py / viewer.py keyPressEvent), never as a window-level QAction shortcut — a bare-letter global shortcut would fire while typing in the search box. key_only ignores modifiers, so it is checked AFTER each surface's exact Ctrl-chord actions (grid.invert's Ctrl+I above all) — same key_only-layering convention as grid.star_toggle's Space vs Shift+Space. |

### Grid (library view)

| Action | Keys | Notes |
| --- | --- | --- |
| `grid.select_all` | *SelectAll* (platform default, e.g. Ctrl+A) | Picasa Ctrl+A; StandardKey = Cmd+A on macOS for free. |
| `grid.hold` | `Ctrl+H` | Picasa: Hold selected photos in Photo Tray (q6l.2). |
| `grid.star_toggle` | `Space` | Picasa: add/remove star on the selected/current photo(s). |
| `grid.deselect` | `Ctrl+D` | Picasa: Deselect photos (ed5.12). Clears the selection set; the current item keeps keyboard focus but is deselected. |
| `grid.invert` | `Ctrl+I` | Picasa: Invert selection (ed5.12). Flips membership for every photo in the current view — selected become unselected and vice versa; the current item stays as keyboard focus. |
| `grid.locate` | `Ctrl+Return`, `Ctrl+Enter` | Picasa: Locate on Disk (Ctrl+Enter). Both Return and keypad Enter spellings. NEW in q6l.8. |
| `grid.open` | `Return`, `Enter` | Open the CURRENT item in the viewer. key_only predates the keymap; grid.locate is matched first so Ctrl+Enter reveals. |
| `grid.clear` | `Esc` | Collapse the selection to the current item (clears all when none). key_only: Esc during leftover modifiers still clears. |
| `grid.next` | `Right`, `J` | J is Picasa's viewer next, NEW in the grid (q6l.8). key_only so Shift+Right/Shift+J extend the selection (handler logic). |
| `grid.prev` | `Left`, `K` | K = prev, as grid.next. |
| `grid.row_down` | `Down` | One visual row down; Shift extends (handler logic). |
| `grid.row_up` | `Up` | One visual row up; Shift extends (handler logic). |
| `grid.first` | `Home` | Jump to the first photo in the current view (ed5.12). key_only so Shift+Home extends the selection from the anchor. |
| `grid.last` | `End` | Jump to the last photo in the current view (ed5.12). key_only so Shift+End extends the selection from the anchor. |

### Hover peek overlay

| Action | Keys | Notes |
| --- | --- | --- |
| `peek.dismiss` | `Esc` | Esc dismisses the peek. key_only is load-bearing: the Ctrl+Alt trigger chord is HELD when Esc arrives (q6l.5). |

### Viewer (single photo)

| Action | Keys | Notes |
| --- | --- | --- |
| `viewer.close` | `Esc`, `Backspace` | Picasa: Esc closes the Photo Viewer; Backspace is our alias. |
| `viewer.zoom_toggle` | `1`, `Ctrl+Alt+0` | Picasa Photo Viewer: 1 = toggle 100% zoom. ARBITRATION: bare '1' is grandfathered on a RESERVED key and cedes to the M2 star-set (0-5); Ctrl+Alt+0 (and click) remain (PR #45). |
| `viewer.hold` | `Ctrl+H` | Hold the shown photo in the tray (q6l.2). |
| `viewer.locate` | `Ctrl+Return`, `Ctrl+Enter` | Picasa: Locate on Disk, from the viewer too. NEW in q6l.8. |
| `viewer.faces` | `F` | Face-region overlay (cam.4) — our binding; Picasa has none. |
| `viewer.pan_left` | `Ctrl+Left` | Quarter-viewport pan, ONLY while zoomed (handler gate); plain arrows keep meaning next/prev (triage loop priority). |
| `viewer.pan_right` | `Ctrl+Right` | As viewer.pan_left. |
| `viewer.pan_up` | `Ctrl+Up` | As viewer.pan_left. |
| `viewer.pan_down` | `Ctrl+Down` | As viewer.pan_left. |
| `viewer.star_toggle` | `Space` | Picasa library shortcut: add/remove star. Fauxcasa stores the override in its own cache; originals remain read-only. |
| `viewer.next` | `Right`, `J` | Picasa: Right/J = next. Checked AFTER pan, so Ctrl+Right pans while zoomed. |
| `viewer.prev` | `Left`, `K` | Picasa: Left/K = prev. |
| `viewer.play_pause` | `P` | Video playback (fauxcasa-v46.3): play/pause the shown video. Our own pick — Picasa's documented video keys are / (pause) and , . (rew/ff), but Space is claimed by star_toggle (the triage loop's key, which must keep working on videos), so P carries play/pause; / , . stay unclaimed (module docstring). |
| `viewer.seek_back` | `Ctrl+Left` | Skip back 5 s during video playback (v46.3). CONTEXT-LAYERED on viewer.pan_left's chord deliberately: pan fires only while zoomed and 1:1 is unsupported for video in v1, seek only while a playback session is live — mutually exclusive by handler gates, encoded in _CONTEXT_LAYERED so conflicts() stays green. |
| `viewer.seek_fwd` | `Ctrl+Right` | Skip forward 5 s during video playback. As viewer.seek_back (context-layered on viewer.pan_right). |

### Slideshow

| Action | Keys | Notes |
| --- | --- | --- |
| `slideshow.pause` | `Space` | Pause/resume the dwell (conventional; corpus documents no Picasa slideshow keys). |
| `slideshow.next` | `Right`, `J` | As the viewer. |
| `slideshow.prev` | `Left`, `K` | As the viewer. |
| `slideshow.exit` | `Esc`, `Backspace` | Exit to the grid. |

## Files this app writes

Fauxcasa never writes into your photo library or any Picasa file. Everything
it writes lives under its own cache directory.

Cache-root files (one set per machine):

- `config.json` — remembered library path, sort/view preferences
- `library.json` — known multi-root library registrations
- `fauxcasa.log` — rotating diagnostic log (~4 MB ceiling)

Per-library files (one set per library, under a library-specific
subdirectory of the cache root):

- `catalog.json` (or `catalog.json.zst`) — the persisted scan catalog
- `thumbs.fcache` and `thumbs.fcache.json` — the thumbnail cache and its
  index
- `stars.json` — your star overrides, kept local, never written to the
  library
- `config.json` — per-library sort mode, flat/tree view choice, File
  Types selections
- `import-report.json` — counts and notes from ingest (no in-app viewer
  yet; see Known limitations)

## Known limitations

- **Decoding is in-process, not sandboxed, on every platform.** A Windows
  AppContainer decode sandbox exists and is exercised in CI, but the app
  does not yet route photo/video decoding through it. Video playback runs
  in a separate helper process, but that process is not OS-sandboxed
  either. Only open libraries whose files you trust.
- At the smallest thumbnail size, fast scrolling of very large libraries on
  4K/high-DPI displays can stutter.
- Face regions stored inside image files (XMP `mwg-rs`), and captions or
  keywords stored inside RAW/TIFF files, are not read.
- Picasa's manual folder sort order is not read.
- Import notes are counted in the status bar and written to
  `import-report.json`, but there is no in-app viewer for them yet.
- Videos have no 1:1 view.
- Starring is not available while a slideshow is running.
- macOS is not built (no hardware available to build/test on).
- The Windows build is unsigned — see SmartScreen above.
- Linux ships as a `tar.gz` built on Ubuntu 22.04 (glibc ≥ 2.35), and needs
  system `libxcb-cursor0 libxkbcommon-x11-0` plus GL/EGL and fontconfig
  libraries; alternatively run from source with
  `uv run apps/desktop-python/main.py ~/Pictures`.

## Where 0.1 stands against the M1 definition

The product spec's M1 gate ("See your library again") has three clauses:

1. **N4 budgets green on the 100k synthetic library.** Mostly green;
   scrolling at minimum zoom on high-DPI displays remains over budget
   (tracked: `fauxcasa-q6l.27`).
2. **`picasa_db.py survey` cross-check shows zero ingest loss on synthetic
   corpora.** Green in CI (`scripts/check-ingest-parity.py`).
3. **Owner confirms the same on the family archive.** Not yet recorded —
   this is a pending owner action (tracked: `fauxcasa-6g8`). See
   `docs/m1-gate-confirmation.md` for the runbook and confirmation record.

## Privacy when reporting a bug

Do not include photos, `.picasa.ini`/`.pal`/`contacts.xml`/db3 file
contents, real names, or real filesystem paths in a bug report. Do
include:

- The output of `fauxcasa.exe --version` (or `fauxcasa-console.exe
  --version`)
- Your OS and version
- A redacted copy of `fauxcasa.log` (strip paths and names before
  pasting)

## Provenance

<!-- appended by CI -->

## Licensing

Fauxcasa uses strong copyleft by default so the work and its community
remain a commons.

- Application code, scripts, tests, and build files:
  `AGPL-3.0-or-later`.
- Project documentation (including these release notes): `CC-BY-SA-4.0`.
- Third-party dependency license notices are bundled inside each release
  archive as `THIRD-PARTY-NOTICES.md`.
- Corresponding source for this release is the tagged commit `v0.1.0` in
  this repository — the AGPL's network-use clause means any modified
  version reachable over a network must offer that source too.

See `LICENSE`, `docs/LICENSE.md`, and `CONTRIBUTING.md` for the full terms.

## Since tracer-test-v1

Highlights since the `tracer-test-v1` preview:

- Video playback with audio, and video poster thumbnails
- Metadata Info panel (`I`)
- Non-blocking first run — the window appears immediately; the library
  walk runs in the background
- Flat and tree folder views in the sidebar
- Folder descriptions (tooltips, all shapes Picasa can express them in)
- zstd-compressed catalog storage
- An app icon and Windows taskbar identity
- Product version identity (`--version`, the `READY` line, window titles)
  replacing the "tracer" placeholder naming
- The seven post-merge bug fixes from PR 115 (starred-view desync,
  variant-keyed star/sort state, malformed `stars.json` tolerance, gallery
  action visibility after reconcile, stuck activity spinner, stale
  inspector after backfill, rich-text label rendering)
- Small fixes from PR 118 (File Types choices surviving a library switch,
  the Windows fcache-replace-while-open reconcile fix, XMP Rating
  overflow tolerance, slideshow prefetch staleness after a catalog swap)
